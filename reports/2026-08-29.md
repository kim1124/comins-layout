# 2026-08-29 Work Report

## 작업 일시

- 2026-08-29 13:17:39 KST

## 요약

- `comins-grid-layout`의 로컬 목표 버전을 `0.2.1`로 변경했다.
- README와 active docs를 현재 public API, transfer, lazy content, event, handle, persistence, browser evidence와 일치시켰다.
- 효과가 없거나 canonical API와 중복된 공개 surface는 런타임 호환성을 유지한 채 `@deprecated` 처리하고 `0.3.0` 제거 목표를 명시했다.
- `DashboardWidgetActionLabels`를 package root type export로 추가했다.
- `/examples/transfer`를 canonical multi-grid route의 compatibility redirect로 전환하고, 기존 `/examples/internal/**` consumer route를 `__test__` regression harness로 분리했다.
- README와 in-app API reference의 prop, command, handle inventory를 실제 TypeScript key 집합과 양방향 검증하도록 강화했다.
- push, PR, tag, GitHub Release, npm publish는 수행하지 않았다.

## 주요 변경 파일

- Package/API: `package.json`, `package-lock.json`, `src/core/types.ts`, `src/components/DashboardGrid.tsx`, `src/components/DashboardWidget.tsx`, `src/gridstack/option-mapper.ts`, `src/index.ts`
- Consumer docs: `README.md`, `CHANGELOG.md`, `docs/01-requirements.md`부터 `docs/06-transfer-lazy-loading-plan.md`, `docs/README.md`
- Examples: `example/src/docs/content.tsx`, `example/src/main.tsx`, current Playground components/routes, `example/src/readme-demo.tsx`
- Contract tests: `test/vitest/public-doc-contract.ts`, `test/vitest/fixtures/public-doc-consumer.tsx`, `test/vitest/readme.test.ts`, affected Playwright specs, `test/security/sensitive-data-gates.node.mjs`
- Plan: `docs/superpowers/plans/2026-08-29-public-api-readme-alignment.md`

## 검증 결과

### Focused type/Vitest

- `npm run typecheck`: 통과
- README, option mapper, configuration, handle, hook, drag-in, transfer, Playwright policy focused Vitest: 74/74 통과
- 최종 README contract Vitest: 7/7 통과

### Browser

- Chromium docs/transfer 재검증: 13/13 통과
- Chromium dashboard interaction regression: 27/27 통과
- Firefox `@firefox-parity`: 8/8 통과
- Mobile Chrome `@mobile-touch`: 4/4 통과
- 최종 Chromium docs/API reference: 9/9 통과
- 최초 Chromium 묶음은 24건 통과, 5건 실패했다. 실패는 canonical transfer 제목과 상위 Advanced menu의 `aria-current`를 이전 route 계약으로 기대한 테스트/UI navigation 계약 불일치였다. 경로 기반 상위 메뉴 표시와 canonical 제목을 수정한 뒤 affected specs를 재실행해 통과했다.

### Baseline/package

- `npm run verify`: 통과
  - security: 25/25
  - license: 통과
  - TypeScript: 통과
  - Vitest: 166/166
  - production build: 통과
- `npm run verify:package-artifact`: `comins-grid-layout-0.2.1.tgz` 통과
- 동일 tarball의 `npm run test:consumer`: 통과
- `npm pack --dry-run --ignore-scripts`: 48개 파일, package version `0.2.1` 확인
- 생성된 tarball과 task-specific 임시 npm cache는 검증 후 제거했다.

## 실패 분류와 조치

- 첫 baseline은 security fixture가 package candidate version을 `0.2.0`으로 고정해 실패했다. 로컬 candidate `0.2.1`과 실제 latest-published support `0.2.0`을 분리해 검증하도록 수정했다. `SECURITY.md`의 published support 표는 publish 전이므로 `0.2.0`을 유지했다.
- 첫 artifact checker 실행은 사용자 npm cache의 권한 오류로 실패했다. task-specific temporary npm cache로 동일 checker를 재실행해 통과했으며 checker 정책은 완화하지 않았다.
- 첫 consumer smoke는 restricted network 상태에서 의존성 설치가 대기해 중단했다. 허용된 네트워크 환경에서 동일 tarball을 설치해 통과했다.

## 미실행 및 잔여 리스크

- `npm run verify:full`과 `chromium-resource`는 실행하지 않았다. 실제 publish 요청이 없고 adapter/resource runtime 동작을 변경하지 않은 문서/API 정합화 작업이므로 affected specs와 baseline만 실행했다.
- branded Edge와 실제 Safari는 직접 검증하지 않았다.
- deprecated API는 `0.2.1`에서 계속 동작한다. 실제 제거는 별도 승인된 `0.3.0` breaking-change 작업으로 남는다.
- npm registry의 latest published version과 `SECURITY.md` 지원 표는 실제 publish 전까지 `0.2.0`이다.

## 0.2.1 공개 릴리스 마감

### 작업 일시

- 2026-08-29 16:25:04 KST

### 공개 결과

- source SHA `3895781fadd127dcf7bc1f963ffa2534c595c202`에서 GitHub Actions의 trusted publisher와 staged publishing으로 `comins-grid-layout@0.2.1`을 공개했다.
- npm `latest` dist-tag는 `0.2.1`을 가리킨다.
- staged approval ID는 `783ff458-c080-46ef-bd18-8207eebe55fb`이다.
- publish workflow run은 `33236809467`, 공개 npm identity 검증 run은 `33240695969`이며 모두 성공했다.
- 공개 package SHASUM은 `2a05e4d80192863adbd8f29755eb0bc7f0b97cef`로 staged artifact와 일치한다.
- 공개 package integrity는 `sha512-l+tbPhU/T/6p4ptmXtIjnFw28UE6ck20NcAbLDjrjKPbrjWPNfcikSjMkbzH2jcm0QLtz310Vh71U+g/d0tRFA==`이다.
- npm ECDSA registry signature와 SLSA provenance attestation이 존재함을 registry metadata로 확인했다.
- 빈 임시 consumer 프로젝트에서 public registry의 정확한 `comins-grid-layout@0.2.1`을 React 18과 함께 신규 설치하고 package root의 30개 export 및 `comins-grid-layout/styles.css` 해석을 확인했다.
- 최신 공개 버전에 맞춰 `SECURITY.md` 지원 범위를 `0.2.1`로 갱신했다.
- source SHA `3895781fadd127dcf7bc1f963ffa2534c595c202`를 가리키는 `v0.2.1` tag와 GitHub Release를 생성했다.

### 원격·로컬 상태 및 잔여 리스크

- 공개 npm artifact의 source SHA와 로컬·원격 `main`은 릴리스 당시 `3895781fadd127dcf7bc1f963ffa2534c595c202`로 일치했다.
- repository 내부의 detached worktree는 요청 범위 밖이므로 삭제하지 않았다.
- branded Edge와 실제 Safari는 직접 검증하지 않았고, deprecated API 제거는 `0.3.0` 후속 작업으로 남는다.
