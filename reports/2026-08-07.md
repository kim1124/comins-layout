# 2026-08-07

## 16:53 KST - 0.2.0 최소 릴리즈 메타데이터 준비

### 요약

- 최신 `origin/main` 커밋 `9266190`에서
  `codex/release-0.2.0-minimal` 격리 작업트리를 생성했다.
- 아직 공개되지 않은 `0.1.6` 운영 변경과 병합 완료된 외부 드롭 타깃,
  Firefox/Playwright WebKit 자동화 범위, 컬럼별 레이아웃 보존 및 Playground
  통합을 `0.2.0` CHANGELOG에 합쳤다.
- package manifest와 lockfile 루트 버전, 지원 버전 표 및 저장소 정책 테스트를
  `0.2.0`으로 정렬했다.
- runtime source, public API/type, CSS, dependency resolution 및 workflow는
  변경하지 않았다.

### 변경 파일

- `package.json`
- `package-lock.json`
- `CHANGELOG.md`
- `SECURITY.md`
- `test/security/sensitive-data-gates.node.mjs`
- `reports/2026-08-07.md`

### 검증

- 변경 전 baseline `npm run verify`: 통과.
  - security `25/25`
  - license 및 TypeScript 검사 통과
  - Vitest `110/110`
  - Vite 및 declaration build 통과
- 변경 후 focused 정책 테스트
  `node --test test/security/sensitive-data-gates.node.mjs`: `6/6` 통과.
- `npm install --package-lock-only --ignore-scripts`: 통과, audit 취약점 `0`.
- 변경 후 `npm run verify`: 통과.
  - security `25/25`
  - license 및 TypeScript 검사 통과
  - Vitest `110/110`
  - Vite 및 declaration build 통과
- 브라우저 동작, runtime source 및 workflow 변경이 없어 로컬 Playwright E2E와
  exact artifact 검사는 이번 focused 단계에서 실행하지 않았다. PR CI와 실제
  publish workflow가 각각 전체 브라우저 및 exact artifact gate를 소유한다.

### 잔여 이슈 및 승인 경계

- merge, publish workflow dispatch, npm 2FA 승인, 공개 배포, tag 및 GitHub
  Release는 이 릴리즈 메타데이터 변경 범위에 포함하지 않는다.
- 실제 Safari는 직접 검증하지 않았으며 현재 지원 문서는 Playwright WebKit을
  호환성 신호로만 취급한다.
- 다음 단계는 릴리즈 PR의 CI와 코드 리뷰를 통과한 뒤 별도 승인으로 main에
  병합하는 것이다.

## 17:37 KST - PR #24 코드리뷰 후속 수정

### 요약

- 코드리뷰에서 CHANGELOG의 `package-owned deletion` 표현이 실제 외부 드롭
  계약과 반대로 읽힐 수 있는 Important 이슈를 확인했다.
- 외부 드롭 callback은 비파괴적이며 consumer가 `removeWidget` 호출 여부를
  결정한다는 공개 계약으로 문구를 교정했다.
- 정책 테스트에 비파괴 callback과 consumer 삭제 소유권 assertion을 추가했다.

### 변경 파일

- `CHANGELOG.md`
- `test/security/sensitive-data-gates.node.mjs`
- `reports/2026-08-07.md`

### 검증

- `node --test test/security/sensitive-data-gates.node.mjs`: `6/6` 통과.
- `npm run verify`: 통과.
  - security `25/25`
  - license 및 TypeScript 검사 통과
  - Vitest `110/110`
  - Vite 및 declaration build 통과

### 잔여 이슈 및 승인 경계

- 수정된 head에서 PR CI를 다시 통과해야 main 병합을 진행할 수 있다.
- publish workflow dispatch, npm 2FA 승인 및 공개 배포는 별도 승인 경계다.

## 18:01 KST - publish Chromium 전용 검증 수정

### 요약

- `0.2.0` publish workflow run `31163338409`는 Chromium만 설치한 상태에서
  Firefox와 WebKit까지 포함하는 `verify:full`을 실행해 E2E `142`건이
  browser executable 미존재로 실패했다.
- `verify-and-pack` 실패로 exact artifact, consumer, stage job은 실행되지
  않았으며 npm Registry 변경은 발생하지 않았다.
- 사용자 결정에 따라 PR CI의 Chromium/Firefox/WebKit 검증은 유지하고,
  publish workflow만 Chromium, mobile-chrome, chromium-resource project를
  실행하도록 제한했다.

### 변경 파일

- `.github/workflows/publish.yml`
- `test/vitest/playwright-project-policy.test.ts`
- `reports/2026-08-07.md`

### 검증

- TDD RED: focused Playwright policy test는 기존 `npm run verify:full`을 감지해
  `4/5` 통과, `1/5` 실패했다.
- TDD GREEN: workflow 수정 후 동일 테스트 `5/5` 통과.
- publish workflow YAML parse: 통과.
- `npm run verify`: 통과.
  - security `25/25`
  - license 및 TypeScript 검사 통과
  - Vitest `111/111`
  - Vite 및 declaration build 통과
- 실제 publish E2E 명령:
  `npm run test:e2e -- --project=chromium --project=mobile-chrome --project=chromium-resource`
  통과.
  - `114 passed`, `29` intentional skipped
  - 100-widget 반복 컬럼 전환 resource 검사 통과

### 잔여 이슈 및 승인 경계

- 원격 push, 수정 PR, merge 및 publish workflow 재실행은 아직 수행하지 않았다.
- 수정 PR의 전체 CI는 Firefox와 WebKit을 포함한 기존 `verify:full`을 유지한다.
- exact artifact, Gitleaks, React 18 consumer 및 npm stage는 수정 merge 후
  publish workflow 재실행에서 검증해야 한다.

## 18:16 KST - PR #25 정책 테스트 코드리뷰 후속 수정

### 요약

- PR #25 코드리뷰에서 publish/PR workflow 구현은 요구사항에 맞지만, 기존
  문자열 부분 일치 정책 테스트가 잘못된 workflow 변형을 놓치는 Important
  이슈를 확인했다.
- workflow의 모든 `run` 명령을 공백 정규화해 추출하고, publish의 baseline,
  Chromium project 목록 및 PR의 `verify:full` 명령을 정확히 비교하도록
  정책 검사를 보강했다.
- baseline 삭제, Chromium project 삭제, Firefox 추가, unfiltered E2E 추가,
  `verify:e2e` alias 추가, 직접 Playwright 실행 및 PR `verify:full` 삭제의
  일곱 회귀 변형을 명시적으로 검증한다.

### 변경 파일

- `test/vitest/playwright-project-policy.test.ts`
- `reports/2026-08-07.md`

### 검증

- TDD RED: 변형 회귀 테스트 추가 후 focused Vitest `5/10` 통과,
  `5/10` 예상 실패.
- TDD GREEN: run 명령 구조 검사 보강 후 focused Vitest `10/10` 통과.
- 재리뷰 TDD RED: `verify:e2e` alias와 직접 Playwright 실행 변형 추가 후
  focused Vitest `10/12` 통과, `2/12` 예상 실패.
- 재리뷰 TDD GREEN: E2E 실행 allowlist 확장 후 focused Vitest `12/12`
  통과.
- publish 및 verify workflow YAML parse: 통과.
- `npm run verify`: 통과.
  - security `25/25`
  - license 및 TypeScript 검사 통과
  - Vitest `118/118`
  - Vite 및 declaration build 통과

### 잔여 이슈 및 승인 경계

- 새 head를 push하면 PR #25의 전체 CI를 다시 통과해야 한다.
- 실제 publish workflow 재실행, npm stage 및 공개 배포는 merge 이후 별도
  승인 경계다.

## 19:28 KST - PR 및 publish 필수 CI Chromium 전용 정렬

### 요약

- PR #25 run `31165620041`의 기본 검증과 정책 체크는 통과했지만, 전체
  E2E에서 WebKit responsive-state 테스트 1건이 비결정적으로 실패했다.
- 해당 runtime은 이전 head CI에서 통과했고 focused WebKit 반복 `25/25`도
  통과했으나, 사용자 지시에 따라 재실행 대신 필수 PR CI 범위를 publish와
  동일한 Chromium 계열로 변경했다.
- PR 및 publish workflow는 `chromium`, `mobile-chrome`,
  `chromium-resource`만 필수 검증한다. Firefox와 WebKit 프로젝트 정의는
  명시적 로컬 비필수 실행용으로 유지한다.
- README와 지원 경계를 Chromium-only 필수 CI 계약에 맞게 수정했다.

### 변경 파일

- `.github/workflows/verify.yml`
- `README.md`
- `docs/05-open-questions.md`
- `test/vitest/playwright-project-policy.test.ts`
- `reports/2026-08-07.md`

### 검증

- workflow TDD RED: focused 정책 테스트 `11/12` 통과, PR workflow의
  Firefox/WebKit 설치를 감지해 `1/12` 예상 실패.
- workflow TDD GREEN: PR workflow Chromium 전용 변경 후 `12/12` 통과.
- 문서 TDD RED: Chromium-only 자동화 문구 미반영을 감지해 `11/12` 통과,
  `1/12` 예상 실패.
- 문서 TDD GREEN: README 및 지원 경계 수정 후 `12/12` 통과.
- publish 및 verify workflow YAML parse: 통과.
- `npm run verify`: 통과.
  - security `25/25`
  - license 및 TypeScript 검사 통과
  - Vitest `118/118`
  - Vite 및 declaration build 통과
- 필수 Chromium E2E 명령 통과.
  - `114 passed`, `29` intentional skipped
  - 100-widget 반복 컬럼 전환 resource 검사 통과

### 잔여 이슈 및 승인 경계

- 새 head를 push하면 PR #25의 Chromium-only CI를 다시 통과해야 한다.
- Firefox/WebKit은 필수 CI 및 릴리즈 게이트에서 제외되며, 명시적으로
  실행하지 않는 한 해당 엔진 호환성을 현재 릴리즈 증거로 주장하지 않는다.
- 실제 publish workflow 재실행, npm stage 및 공개 배포는 merge 이후 별도
  승인 경계다.
