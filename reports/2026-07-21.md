# 2026-07-21

## Contract v1.2 Minimal Sensitive-Data Gates

### Work Time

- `2026-07-21 20:31-20:40 KST`

### Summary

- Adopted the concise Comins Contract v1.2 sensitive-data policy without changing product source.
- Added Gitleaks 8.30.1 for staged, push-range, CI-range, and extracted-package scanning.
- Added the reviewed 68-line raw public Git identity checker and 48-line exact package-artifact validator.
- Replaced mutable workflow action tags with immutable checkout and Node setup commits.
- Changed staged publishing to upload and publish the same inspected package archive.
- Added a rule-specific npm package/version coordinate allowlist after the extracted README produced a confirmed email-rule false positive.

### Changed Files

- Policy: `AGENTS.md`, `SECURITY.md`
- Shared gates: `.gitleaks.toml`, `.githooks/pre-commit`, `.githooks/pre-push`, `scripts/check-public-identities.mjs`
- Package gate: `scripts/verify-package-artifact.mjs`, `test/security/package-artifact-gate.node.mjs`
- CI and release: `.github/workflows/verify.yml`, `.github/workflows/publish.yml`, `package.json`, `.gitignore`
- Contract tests: `test/security/sensitive-data-gates.node.mjs`

### Verification

- RED/PASS: the new Node contract suite failed 7/7 before implementation and passed 7/7 after implementation.
- PASS: `npm run verify`, including security checks, 19 Vitest tests, TypeScript lint, and production build.
- PASS: `npm run test:consumer`.
- PASS: the exact `comins-grid-layout` package artifact matched `package.json#files` and the extracted artifact passed Gitleaks directory scanning.
- PASS: both hook scripts passed shell syntax checks and both workflows passed YAML parsing.
- PASS: `git diff --check`.
- PASS: no generated package archive or raw scanner output remains.

### Residual Risks

1. GitHub required checks, Push Protection, and the updated workflows are not changed or executed remotely.
2. Legacy public history and provider-retained metadata remain separate remediation and are not hidden by a baseline.
3. The tracked hook is not installed into shared linked-worktree Git configuration.
4. Browser-visible product behavior did not change, so Playwright was not rerun for this policy/tooling-only task.
5. No push, pull request, release, publish, tag, or provider-setting change was performed.
