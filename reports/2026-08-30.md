# 2026-08-30 Work Report

## 작업 일시

- 2026-08-30 KST

## 요약

- `comins-grid-layout@0.2.1` README를 현재 공개 기능 중심으로 개편했다.
- 설치 명령 바로 아래에 source checkout 기준 데모 실행 명령과 로컬 URL을 추가했다.
- Palette/Grid transfer, external HTML drop, per-column persistence, React content lazy rendering을 실제 공개 API로 구동하는 독립 README 데모 4종과 Chromium 회귀 테스트를 추가했다.
- 검증된 UI 상호작용을 각각 독립 GIF로 캡처하고, 크기·시간·프레임·무한 반복 조건을 모두 통과한 경우에만 기존 자산을 원자적으로 교체하도록 생성 스크립트를 갱신했다.
- 기존 단일 CRUD GIF는 제거했다. package runtime, public API, dependency, version, lockfile은 변경하지 않았다.
- Data Table 문서 운영 기준에 맞춰 `docs/user` 영문 primary 가이드 14개와 동일 파일 구조의 `docs/ko` 국문 가이드 14개를 추가했다.
- 각 가이드에 public API 사용 예제, 제어 상태 write-back 위치, 거부 조건, 미지원 경계, canonical Playground route를 명시했다.
- README의 잘못된 `/examples/basic/add-remove`, `/docs/api` 경로를 실제 `/examples/widget/manage`, `/api`로 교정하고 npm에서 열리는 절대 GitHub 가이드 링크를 추가했다.
- package version은 `0.2.1`로 유지하며 push, PR, merge, tag, Release, npm publish는 수행하지 않는다.

## 변경 파일

- 문서: `README.md`, `docs/README.md`, `docs/user/*.md`, `docs/ko/*.md`
- 계획: `docs/superpowers/plans/2026-08-30-{readme-feature-highlights,user-guides}.md`
- 데모: `example/src/readme-demo.tsx`, `example/src/styles.css`
- GIF: `docs/assets/comins-grid-layout-{transfer,external-drop,responsive-persistence,lazy-rendering}.gif`
- 생성·검사: `scripts/capture-readme-demo.mjs`, `scripts/inspect-readme-gif.mjs`, `scripts/inspect-readme-gif.d.mts`
- 테스트: `test/vitest/{readme,user-docs}.test.ts`, `test/playwright/specs/readme-demo.spec.ts`
- 제거: `docs/assets/comins-grid-layout-demo.gif`

## 수행한 검증

- README contract RED: 신규 섹션·데모 명령·4개 GIF 부재로 3건 실패 확인
- 사용자 가이드 contract RED: 28개 가이드 부재, 잘못된 route, README guide link 부재 등 6건 실패 확인
- README 및 사용자 가이드 focused Vitest: 2 files / 17 tests 통과
  - 영문·국문 14개 동일 파일 구조
  - 현재 public prop/command/handle 전체 명시
  - deprecated alias의 canonical TSX 예제 유입 차단
  - README 절대 GitHub 링크 및 실제 example application route 대조
  - 가이드 간 상대 링크 대상 존재 확인
- 기능별 README Chromium E2E: 4/4 통과
- 기존 `/readme-demo` 회귀와 신규 기능별 데모 Chromium 묶음: 31/31 통과
- `npm run docs:readme-gif`: 4개 자산 원자적 재생성 및 메타데이터 검증 통과
- `npm run verify`: 통과
  - security: 25/25
  - license: 통과
  - TypeScript: 통과
  - Vitest: 18 files / 176 tests 통과
  - production build: 통과
- GIF 메타데이터:
  - transfer: 960x720, 46 frames, 3.68s, 141,076 bytes, infinite loop
  - external drop: 960x720, 34 frames, 2.72s, 90,717 bytes, infinite loop
  - responsive persistence: 960x720, 36 frames, 2.88s, 53,270 bytes, infinite loop
  - lazy rendering: 960x720, 32 frames, 2.56s, 163,483 bytes, infinite loop
- `git diff --check`: 통과

초기 security 단독 실행 2건은 제한된 shell PATH에 `npm`과 `node`가 없어 자식 프로세스가 시작되지 않은 환경 실패였다. NVM Node/npm 경로를 명시한 동일 security gate와 최종 `npm run verify`에서 모두 통과했다.

## 결과 및 잔여 리스크

- README의 설명·예제·로컬 데모 경로가 현재 `0.2.1` 공개 surface와 일치한다.
- 사용자 가이드는 Git 저장소 문서이며 npm package `files` 목록은 변경하지 않았다. 이미 publish된 npm `0.2.1`의 README는 새 publish 없이는 교체되지 않는다.
- 실제 Safari와 branded Edge는 이번 문서 작업에서 추가 검증하지 않았으며 README의 미검증 경계로 명시했다.
- `npm run verify:full`은 publication 또는 명시적 maintainer 요청 범위이므로 실행하지 않았다.
- 변경은 로컬 브랜치 `codex-readme-feature-highlights`의 문서 및 문서 재현 fixture로만 커밋하고 외부 쓰기는 수행하지 않는다. Package runtime `src/**`는 변경하지 않는다.

## 21:56 KST - 0.2.2 문서 신뢰성 및 최적화 검토

### 요약

- README에서 실행 중인 로컬 서버가 없으면 실패하는 localhost 링크 9개를 source checkout 전용 route 표기로 교정했다.
- README, 영문 가이드, 국문 가이드, 공개 타입 JSDoc의 patch 호환성 설명을 특정 `0.2.1` 고정 표현에서 `0.2.x` 계약으로 정렬하고, 기존 `0.3.0` 제거 예고는 유지했다.
- 저장소 내부 파일·Markdown anchor·same-repository GitHub target·공개 example route를 현재 구현과 대조하는 문서 계약 테스트를 추가했다.
- 문서 전용 CI job에 README, 영문/국문 가이드, 링크 계약 검사를 모두 포함했다.
- 100-widget resource 기준선을 다시 측정했다. 누수나 병목 증거가 없어 package runtime 최적화 변경은 수행하지 않았다.
- package version은 `0.2.1`로 유지했으며 commit, push, publish는 수행하지 않았다.

### 변경 파일

- 문서: `README.md`, `docs/04-verification-strategy.md`, `docs/user/{03,09,11}-*.md`, `docs/ko/{03,09,11}-*.md`
- 공개 타입 설명: `src/core/types.ts`
- 검사·CI: `test/vitest/document-links.test.ts`, `.github/workflows/verify.yml`
- 작업 보고: `reports/2026-08-30.md`

### 수행한 검증

- 문서 링크 계약 RED: README의 clickable localhost link 9개와 누락된 `0.2.x` 호환 표현으로 2/4 실패를 확인했다.
- 문서 focused Vitest: 4 files / 22 tests 통과.
- `npm run verify`: 통과.
  - security: 25/25
  - license: 통과
  - TypeScript: 통과
  - Vitest: 19 files / 180 tests
  - production build: 통과
- focused Chromium resource gate: 1/1 통과.
  - repeated column cycles: DOM Nodes 14,515 / Event Listeners 1,013 / Documents 1 고정
  - repeated interactions: DOM Nodes 14,527 / Event Listeners 1,026 / Documents 2 고정
  - post-GC heap transient peak 및 trailing steady-state budget 통과
- 외부 URL 현재 응답 점검:
  - npm/TypeScript/license Shields 3개: HTTP 200
  - GitHub Private Vulnerability Reporting: 인증 화면으로 redirect 후 HTTP 200
  - npm package web page: 자동 요청 HTTP 403, official registry latest metadata HTTP 200

최초 Chromium resource 실행은 제한된 sandbox에서 `127.0.0.1:6001` port bind가 거부되어 환경 실패로 분류했다. 포트 실행이 허용된 동일 로컬 환경에서 같은 명령을 재실행하여 통과했다.

### 결과 및 잔여 리스크

- deterministic 문서 계약은 CI에서 자동 검증한다. 외부 사이트는 네트워크·인증 상태가 비결정적이므로 release-time/manual 확인 대상으로 유지한다.
- npm package web page는 자동 요청 차단 때문에 실제 브라우저 접근을 이번 실행에서 확정하지 않았다.
- 런타임 resource counter가 기존 예산 내에서 안정적이므로 근거 없는 최적화는 회귀 위험만 늘린다고 판단했다.
- version bump, changelog release entry, package artifact 검증, 원격 CI는 실제 `0.2.2` release 준비 승인 전까지 수행하지 않았다.

## 22:08 KST - 0.2.2 Candidate 메타데이터 준비

### 요약

- 문서 신뢰성·검증 강화 변경을 public API와 runtime behavior가 유지되는 patch release `0.2.2`로 분류했다.
- `package.json`, root lockfile metadata, `CHANGELOG.md`, `SECURITY.md`를 `0.2.2` Candidate 기준으로 정렬했다.
- 현재 API reference와 Playground의 compatibility 설명을 `0.2.2` 또는 `0.2.x` 계약에 맞췄다. `0.2.1`에 deprecated가 시작됐다는 이력과 `0.3.0` 제거 예정은 유지했다.
- `publish.yml`이 `verify:full`, exact artifact 생성·검사, consumer smoke, trusted staging을 소유하므로 별도 로컬 tarball은 생성하지 않았다.

### 변경 파일

- 버전·릴리스: `package.json`, `package-lock.json`, `CHANGELOG.md`, `SECURITY.md`
- 활성 문서·Playground: `docs/README.md`, `docs/03-component-api-draft.md`, `docs/06-transfer-lazy-loading-plan.md`, `example/src/docs/content.tsx`
- 보안 계약: `test/security/sensitive-data-gates.node.mjs`

### 승인 경계

- commit, push, PR, merge, tag, GitHub Release, npm stage/publish는 수행하지 않는다.
- exact artifact와 consumer smoke는 병합된 reviewed source를 대상으로 canonical publish workflow에서 생성·실행한다.

### 수행한 검증

- version/security focused Node test: 6/6 통과
- 문서 focused Vitest: 4 files / 22 tests 통과
- `npm run verify`: 통과
  - security: 25/25
  - license: 통과
  - TypeScript: 통과
  - Vitest: 19 files / 180 tests
  - production build: 통과
- Chromium docs Playground routing: 9/9 통과
- npm registry: `latest=0.2.1`, `0.2.2` metadata HTTP 404로 Candidate 좌표 미게시 확인

최초 registry latest 조회 중 제한된 sandbox DNS 해석이 한 번 실패했다. 네트워크 조회가 허용된 환경에서 동일 official registry endpoint를 다시 확인하여 `0.2.1`을 얻었다.

### 잔여 리스크

- `npm run verify:full`, exact artifact 검사, consumer smoke, 원격 CI는 아직 실행하지 않았다. 이들은 reviewed source가 main에 병합된 뒤 canonical `publish.yml`에서 실행한다.
- `SECURITY.md`의 `0.2.2` 지원 표는 Candidate가 실제로 publish되는 것을 전제로 한 release metadata다. publish되지 않으면 public default branch에 반영해서는 안 된다.

## 23:42 KST - 0.2.2 Playground 안내 및 상호작용 보완

### 요약

- 27개 Playground route를 기능 설명, 세부 기능, 선택적 코드 예제, GridStack 컨트롤, GridStack 실행 예제의 공통 구조로 정리했다.
- Widget 기본 크기를 W2/H2로 조정하고 N, W, H, 이동 가능 여부, 리사이즈 가능 여부를 한글·영문 설정 표로 표시했다.
- 이동·리사이즈 제어 아이콘을 기능 의미에 맞게 교체하고 W2에서도 제목과 설정 표가 잘리지 않도록 compact layout을 적용했다.
- Title Drag와 세 Nested Grid 예제의 실제 drag handle 영역을 제목 전체 너비로 확장했다. 기존 구현은 drag engine 문제가 아니라 제목 텍스트 36.48px만 handle로 노출되어 사용 가능한 hit area가 지나치게 작았다.
- Nested Basic, Advanced, Constraints와 세 Responsive 예제의 차이를 각각 설명하고, Static Grid와 레이아웃 잠금/해제의 계약 차이를 명시했다.
- 안전한 공개 핸들 예제에 조회·제어 메서드 목록과 raw GridStack 인스턴스 사용 경계를 추가했다.
- Responsive 토글이 실제 옵션을 비활성화하고 12열로 복구한 뒤 재활성화 시 현재 폭에 맞춰 다시 계산하도록 수정했다.

### 변경 파일

- 공통 안내·설정 표: `example/src/playground/components/ExampleGuide.tsx`, `example/src/playground/components/WidgetSettingsTable.tsx`
- 예제: `example/src/playground/{WidgetExamples,LayoutExamples,AdvancedExamples,TransferPlayground}.tsx`
- Widget fixture·표현: `example/src/playground/components/ExampleDashboard.tsx`, `example/src/playground/fixtures.ts`, `example/src/styles.css`
- 브라우저 계약: `test/playwright/specs/playground.spec.ts`, `test/playwright/specs/playground-interaction-contract.spec.ts`
- 릴리스 설명·브라우저 메타데이터: `CHANGELOG.md`, `example/index.html`

### 수행한 검증

- 회귀 테스트 RED:
  - Title Drag handle 너비 36.48px로 기대 범위 미달
  - 공통 기능 안내 영역 부재
  - Widget 기본 너비 W3로 W2 계약 불일치
- `npm run verify`: 통과
  - security: 25/25
  - license: 통과
  - TypeScript: 통과
  - Vitest: 19 files / 180 tests
  - production build: 통과
- 영향 범위 Chromium E2E: 46/46 통과
  - 27개 route 안내 구조와 영문 locale
  - Widget W2/H2 설정 표·아이콘·상태 반영
  - Title Drag, Nested 3종, 일반 route 이동·리사이즈
  - Static Grid 잠금 계약, Responsive 비활성화·복구
- Playwright CLI 시각 확인:
  - Widget 제목·아이콘·설정 표 전체 노출
  - Nested 3단계와 leaf 설정 표 전체 노출
  - 새 브라우저 session console error 0, warning 0
- `git diff --check`: 통과

제한된 shell에서 NVM 경로가 전달되지 않아 최초 `npm run verify`가 `npm: command not found`로 종료되었다. 설치된 Node/npm 경로를 명시한 동일 gate는 통과했다. 최초 Chromium 묶음도 macOS sandbox의 Mach port 권한 거부로 브라우저가 시작되지 않았으며, 권한이 허용된 동일 조건에서 46/46 통과했다. 두 실패 모두 제품 동작과 무관한 실행 환경 실패로 분류했다.

### 결과 및 잔여 리스크

- package public API와 `src/**` runtime은 변경하지 않았다. 변경 범위는 Playground UX, 안내 콘텐츠, 예제 토글 동작과 그 검증 계약이다.
- Firefox·mobile-touch 전체 gate와 publication용 `npm run verify:full`은 실행하지 않았다. 실제 배포 단계에서 canonical workflow가 검증한다.
- 변경은 로컬 브랜치 `codex-playground-guidance-0.2.2`에만 있으며 commit, push, PR, merge, npm publish는 수행하지 않았다.
- 배포 완료 전까지 `http://127.0.0.1:6001` Playground를 유지한다.
