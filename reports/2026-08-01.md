# 2026-08-01

## 21:18 KST - Firefox and WebKit verification support

### Summary

- Added dedicated Playwright desktop projects for Firefox and WebKit while
  retaining Chromium desktop, Pixel 7 Chromium touch, and the isolated
  Chromium CDP resource project.
- Reclassified desktop rendering, pointer interaction, external-drop, and
  Playground scenarios through one explicit project policy instead of
  Chromium-only skips.
- Updated GitHub Actions to install Chromium, Firefox, and WebKit before the
  full browser gate.
- Corrected the grid-area exit fixture so its release point remains inside the
  browser viewport; browser-boundary exit remains covered by separate tests.
- Documented Firefox support and Playwright WebKit engine compatibility without
  claiming direct branded Safari verification.

### Changed Files

- Browser policy and CI:
  - `playwright.config.ts`
  - `.github/workflows/verify.yml`
  - `test/playwright/project-policy.ts`
- Browser and policy tests:
  - `test/playwright/specs/dashboard-grid.spec.ts`
  - `test/playwright/specs/docs-playground-routing.spec.ts`
  - `test/vitest/playwright-project-policy.test.ts`
- Documentation:
  - `README.md`
  - `docs/04-verification-strategy.md`
  - `docs/05-open-questions.md`
  - `test/playwright/README.md`
  - `test/playwright/specs/README.md`

### TDD And Browser Diagnosis

- Policy RED first failed because `test/playwright/project-policy.ts` did not
  exist, then failed for the absent Firefox/WebKit projects and the remaining
  Chromium-only desktop skips.
- Focused policy GREEN passed 4/4 tests after adding the project matrix, CI
  install contract, desktop classifier, and documentation boundary.
- Initial Firefox full run passed 46 tests, skipped 3 mobile-only tests, and
  failed the grid-area drag exit once. A five-repeat run reproduced the same
  failure 5/5 times.
- Trace evidence showed the grid-area test releasing at x=1328 in a 1280px
  viewport. Changing the grid exit offset from 80px to 16px kept the pointer
  outside the grid but inside the viewport.
- The corrected grid-area resize and drag scenarios passed 20/20 across
  Firefox and WebKit with five repeats per engine.
- Firefox full rerun passed 47 tests with 3 intentional mobile-only skips.
  WebKit full discovery passed 47 tests with the same 3 skips.
- External-drop, browser-boundary, and grid-area scenarios passed 72/72 across
  Firefox and WebKit with three repeats per engine.

### Full Verification

- `npm run verify:full`: passed.
  - Security: 25/25 passed.
  - License, TypeScript, and build gates: passed.
  - Vitest: 13 files and 73/73 tests passed.
  - Playwright: 166 passed, 35 intentionally skipped, 0 failed across five
    projects.
  - The isolated 100-widget Chromium resource gate passed its DOM, listener,
    document, heap peak, and steady-state checks.
- `npm run test:consumer`: passed in a fresh React 18 consumer installation.
- `npm ls --depth=0`: passed.
- `git diff --check`: passed.
- `package.json` and `package-lock.json` are unchanged from `origin/main`; no
  dependency or package version changed.

### Residual Scope

- Playwright WebKit is an automated engine-compatibility signal. Direct macOS
  and iOS branded Safari device verification remains outside this gate.
- CDP resource counters and synthetic touch injection remain intentionally
  Chromium-only.
- No `0.2.0` version bump, tag, GitHub Release, npm staging, or publish action
  is included in this change.
