# 2026-07-15

## PVR Security Reporting Alignment

### Summary

- Added the MIT license file required by the package metadata.
- Added a repository-local security policy for GitHub Private Vulnerability Reporting.
- The maintainer confirmed Private Vulnerability Reporting is enabled for `kim1124/comins-layout`.

### Changed Files

- `LICENSE`
- `SECURITY.md`
- `reports/2026-07-15.md`

### Verification

- PASS: `git diff --check` after documentation changes.
- CONFIRMED BY MAINTAINER: GitHub Private Vulnerability Reporting is enabled.

### Residual Risks

- The GitHub PVR setting is maintained remotely and was not queried through the local GitHub CLI in this change.
- Existing staged standalone-documentation changes remain outside this security-policy commit.

## Comins Contract v1.1 Adoption

### Summary

- Adopted the v1.1 shared contract in root AGENTS guidance without changing nested module guidance.
- Replaced mutable dependency tags with existing lockfile-resolved version ranges.
- Documented the GridStack browser runtime boundary and pre-release security prerequisites.

### Changed Files

- `AGENTS.md`
- `README.md`
- `SECURITY.md`
- `package.json`
- `package-lock.json`
- `reports/2026-07-15.md`

### Verification

- PASS: `npm run verify` (`5` test files, `19` tests, production build).
- PASS: `npm --cache /tmp/comins-grid-layout-npm-cache pack --dry-run --json` (`28` package files).
- PASS: package manifest and lockfile root match with no mutable dist-tags.
- PASS: `git diff --check` and `git diff --cached --check`.

### Residual Risks

- GitHub dependency and secret-scanning alert settings remain remote configuration and were not queried or changed locally.
- The pre-existing staged documentation changes remain staged; this contract-adoption hunk is intentionally unstaged.
- The default npm cache has root-owned files, so package inspection used a temporary cache; this is an execution-environment issue, not a package failure.

## Codex Model Policy

### Summary

- Set `gpt-5.6-sol` with `xhigh` reasoning as the default for Comins work.
- Require at least `xhigh` for vulnerability investigation, runtime memory failures, and security work.
- Require at least `max` for instruction planning, Plan mode, and implementation-plan authoring or updates.

### Changed Files

- `AGENTS.md`
- `reports/2026-07-15.md`

### Verification

- PASS: global Codex configuration parsed with `codex --strict-config --version` (`0.144.4`).
- PASS: Comins root instruction policy consistency check and `git diff --check`.

### Residual Risks

- Model availability and supported reasoning levels remain dependent on the active Codex release and account access.

## Contract v1.1 Operational Closure

### Summary

- Committed the pre-existing standalone instruction-surface changes separately as `671b006` before staging Contract v1.1 work.
- Added immutable GitHub Actions CI for the full package and browser gate.
- Added a consumer installation smoke test, package changelog, documented React/Chromium/SSR boundaries, and included the changelog in package contents.
- Added a manual, `main`-only `publish.yml` that uses the GitHub `npm` environment, OIDC, exact version validation, existing-package validation, and stage-only publication.
- Documented the one-time interactive npm bootstrap required before trusted or staged publishing can be registered.

### Changed Files

- `.github/workflows/verify.yml`
- `.github/workflows/publish.yml`
- `AGENTS.md`
- `CHANGELOG.md`
- `README.md`
- `SECURITY.md`
- `package.json`
- `package-lock.json`
- `scripts/consumer-smoke.mjs`
- `reports/2026-07-15.md`

### Verification

- PASS: both workflow YAML files parsed with Ruby Psych.
- PASS: `npm run verify:full` with localhost binding enabled (`5` Vitest files, `19` tests, production build, `51` Playwright tests passed, `10` mobile-only skips).
- PASS: the isolated `chromium-resource` project recorded stable 100-widget heap, DOM node, listener, and document counters.
- PASS: `npm run test:consumer` in an unrestricted network environment; the temporary ESM consumer installed `8` packages and imported the package plus stylesheet export.
- PASS: `npm --cache /tmp/comins-grid-layout-npm-cache pack --dry-run --json` (`29` package files, including `LICENSE`, `README.md`, `CHANGELOG.md`, JavaScript, types, and CSS).
- PASS: `npm view comins-grid-layout ...` returned registry `E404`, confirming that the bootstrap publication has not occurred.
- PASS: `git diff --check`.

### Environment Classification

- The first `npm run verify:full` attempt failed because the sandbox denied `127.0.0.1:6001` with `listen EPERM`.
- The identical command passed after local binding was allowed; this was an execution-environment failure, not a product or test-contract failure.

### Residual Risks

- The first npm publication still requires an explicit maintainer action with npm 2FA; it was not performed.
- After bootstrap, npm must register `kim1124/comins-layout`, `publish.yml`, and the `npm` environment as the trusted publisher with only `npm stage publish` allowed.
- GitHub dependency alerts, secret scanning, push protection, and `npm` environment reviewers remain remote settings that were not readable through the available GitHub connector and were not changed.
- Firefox and Safari remain outside the supported browser contract until Firefox and WebKit Playwright projects are added.

## Release Bootstrap Completion

### Work Time

- `2026-07-15 18:04 KST`

### Summary

- Published `comins-grid-layout@0.1.0` to the public npm registry with maintainer WebAuthn approval.
- Registered `kim1124/comins-layout`, `publish.yml`, and the GitHub `npm` environment as the trusted publisher with only `npm stage publish` permission.
- Set package publishing access to require 2FA and disallow traditional publishing tokens.
- Enabled GitHub dependency alerts and Dependabot security updates; confirmed secret scanning and push protection are enabled.
- Created the GitHub `npm` environment with `kim1124` as the required reviewer and `main` as the only deployment branch.

### Changed Files

- `reports/2026-07-15.md`

### Verification

- PASS: `npm view comins-grid-layout@0.1.0 ...` returned version `0.1.0`, the expected repository, and the published integrity digest.
- PASS: registry installation added `6` packages and imported the package and stylesheet exports.
- PASS: `npm trust list comins-grid-layout` returned repository `kim1124/comins-layout`, workflow `publish.yml`, environment `npm`, and `stage publish` permission only.
- PASS: `npm access set mfa=publish comins-grid-layout` completed with WebAuthn approval.
- PASS: GitHub Verify rerun completed successfully for commit `e0c5e09` after one non-reproduced Chromium pointer-interaction failure; the focused local test passed `10/10` repetitions.

### Residual Risks

- The bootstrap publication was interactive and therefore has no OIDC provenance; future staged releases use trusted publishing and receive provenance automatically.
- The GitHub environment permits self-review because `kim1124` is currently the only administrator; this is a manual approval gate, not separation of duties.
- GitHub repository rulesets remain unconfigured and were outside this release-bootstrap scope.
- Firefox and Safari remain outside the supported browser contract until Firefox and WebKit Playwright projects are added.
- The default npm cache still contains root-owned files; release verification used isolated temporary caches without changing home-directory ownership.

## Repository Privacy Remediation and 0.1.1 Patch

### Work Time

- `2026-07-15 18:35 KST`

### Summary

- Replaced identifying local absolute paths with `<repo-root>` and `$CODEX_HOME` placeholders across active documentation and historical worklogs.
- Updated the README publishing note to reflect the completed trusted-publisher bootstrap.
- Bumped the package from `0.1.0` to `0.1.1` for a corrected public tarball.
- Configured future commits and the rewritten public history to use the authenticated GitHub noreply identity.

### Changed Files

- `README.md`
- `GUIDE.md`
- `docs/README.md`
- `CHANGELOG.md`
- `package.json`
- `package-lock.json`
- `reports/2026-07-10.md`
- `reports/2026-07-14.md`
- `reports/2026-07-15.md`

### Verification

- PASS: `npm run verify:full` (`5` Vitest files, `19` tests, production build, `51` Playwright tests, `10` mobile-only skips).
- PASS: the `chromium-resource` project retained stable 100-widget heap, DOM node, listener, and document counters.
- PASS: `npm run test:consumer` installed `0.1.1` in an isolated ESM consumer and imported the package and stylesheet exports.
- PASS: `npm --cache /tmp/comins-layout-privacy-npm-cache pack --dry-run --json` (`29` package files).
- PASS: current tracked-file scan found no personal Gmail, real name, or identifying local absolute path.
- ENVIRONMENT: the first browser gate attempt failed with sandbox `listen EPERM` on `127.0.0.1:6001`; the identical full gate passed with local binding allowed.
- PASS: all reachable `main` commits use `kim1124` and the authenticated GitHub noreply email for both author and committer metadata.
- PASS: all reachable text blobs on `main` contain no personal Gmail, real name, identifying local path, known token format, high-entropy secret assignment, or non-placeholder email.
- PASS: the rewritten `main` tree is byte-identical to the already verified pre-rewrite tree.
- PASS: replaced remote `main` with the rewritten history and verified local `main`, `origin/main`, and the trusted-publish workflow head at `c06f11961044ba54972ed2ca7061b7a5104b2029`.
- PASS: GitHub Actions trusted-publish run `29405935684` completed all verification, consumer, package, and `npm stage publish` steps successfully.
- PASS: approved npm stage `3215d9f5-4c0b-4e07-b228-4fc6e5115fd4`; `comins-grid-layout@0.1.1` is published with SLSA provenance.
- PASS: staged and published tarballs have the identical SHA-1 digest `74feb773fcabb3a5222a67c5ae962e88d2d59a6d`.
- PASS: the fresh published `0.1.1` tarball contains `29` files and no suspicious filename, personal email, identifying local path, environment variable, generic secret assignment, or high-risk token finding.

### Residual Risks

- The immutable `0.1.0` tarball will continue to expose the former local path in its README.
- Rewritten commits are no longer reachable from a branch, but GitHub still serves former commit objects when addressed directly by their SHA; permanent cached-view cleanup requires GitHub Support review.
- Existing external clones can retain or reintroduce the former history and must be recloned or rebased onto the rewritten `main`.
- The GitHub account profile still exposes its public email until the account-wide email privacy settings are changed.
