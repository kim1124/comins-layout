# 2026-07-23

## 10:17 KST - 100-widget verification guidance and diagnostics alignment

### 요약

- Governance의 최신 `comins-reference` 관리 블록과 `.codex/config.toml`을 동기화했다. Contract는 v1.2로 유지되며 동기화 전후 repository diff는 없었다.
- 실제 adaptive resource test와 불일치하던 `docs/04-verification-strategy.md`를 현재 계약에 맞췄다.
- 100-widget resource assertion에 phase, CDP metric, heap budget, counter artifact를 명시해 GitHub Actions 실패 원인을 로그에서 바로 분류할 수 있도록 했다.
- 12% transient peak, trailing 3-sample 2% final-growth, DOM counter 고정 조건과 합격 판정 로직은 변경하지 않았다.

### 변경 파일

- `docs/04-verification-strategy.md`
- `test/playwright/specs/dashboard-grid.spec.ts`
- `reports/2026-07-23.md`

### 검증

- `node --test test/security/sensitive-data-gates.node.mjs`: 6/6 통과.
- `codex --strict-config --version`: 설정 parse 통과. 샌드박스의 PATH alias 생성 경고는 제품 및 repository 설정 실패가 아니다.
- `npm run test:run -- test/vitest/resource-stability.test.ts`: 6/6 통과.
- `npm run lint`: 통과.
- Focused `chromium-resource` 100-widget test: 1/1 통과. Column/interaction 구간의 Documents, DOM Nodes, Event Listeners 고정 및 기존 heap budget 통과.
- 최초 `npm run verify:full`: 보안 8/8, TypeScript, Vitest 32/32, build까지 통과한 뒤 샌드박스의 `127.0.0.1:6001` bind `EPERM`으로 Playwright web server 시작 실패. 실행 환경 실패로 분류했다.
- 포트 바인딩이 허용된 환경의 `npm run verify:full`: 보안 8/8, TypeScript, Vitest 32/32, build, Playwright 56 passed/13 intentional skipped 및 100-widget resource gate 통과.
- `git diff --check`: 통과.

### 잔여 리스크

- 이번 범위의 확인된 제품 또는 테스트 계약 잔여 리스크는 없다.
- GitHub Actions의 실제 실패 화면에서 새 assertion 문구가 노출되는지는 향후 실제 실패가 발생할 때 확인되는 운영 가시성 항목이며, 이를 검증하기 위해 의도적인 CI 실패를 만들지는 않았다.
- Push, PR, publish, tag, GitHub Release는 수행하지 않았다.

## 10:58 KST - 0.1.5 GridStack 13 public API and license boundary

### 요약

- 외부 GridStack 의존성을 13.0.1로 올리고 nullable 초기화, 공개 메서드, 반응형 `columnOpts` 계약에 맞게 package adapter를 갱신했다.
- `engineOptions`, 반응형 열, 실제 열 변경 콜백, 원자적 `applyLayoutSnapshot`, drag/resize start-stop 이벤트를 공개 API에 추가했다.
- `refresh()`의 기존 compact 부작용을 제거하고, 명시적 `compact()`와 `commitLayout()`을 통해 GridStack public instance 사용 결과를 controlled React state에 반영하도록 분리했다.
- runtime/peer dependency를 bundle external로 고정하고 `THIRD_PARTY_NOTICES.md`, 설치 SPDX/LICENSE, exact npm artifact를 fail-closed로 검증하는 gate를 추가했다.
- package, lockfile, changelog, security support table을 0.1.5로 준비하고 README/API 문서와 소비자 예제를 실제 공개 계약에 맞췄다.

### 주요 변경 파일

- `src/components/DashboardGrid.tsx`
- `src/core/configuration.ts`, `src/core/layout-state.ts`, `src/core/types.ts`, `src/core/use-dashboard-grid.ts`
- `src/gridstack/adapter.ts`, `src/gridstack/option-mapper.ts`
- `THIRD_PARTY_NOTICES.md`, `package-boundary.mjs`, `scripts/check-third-party-notices.mjs`
- `README.md`, `CHANGELOG.md`, `SECURITY.md`, `docs/03-component-api-draft.md`, `docs/05-open-questions.md`
- `example/src/readme-demo.tsx`
- `test/security/*`, `test/vitest/*`, `test/playwright/specs/dashboard-grid.spec.ts`

### 검증

- Focused Vitest: 구성, mapper, 원자적 state, handle 타입 32/32 통과.
- Focused Chromium: handle commit, explicit compact, runtime drag handle, responsive state, drag/resize callback ordering 5/5 통과.
- `npm run verify`: security 15/15, third-party license gate, TypeScript, Vitest 49/49, production build 통과.
- `npm run test:consumer`: React 18 격리 소비자 tarball 설치 및 package/style export smoke 통과.
- `env npm_config_cache=/tmp/comins-grid-layout-npm-cache-0.1.5 npm run verify:package-artifact`: `comins-grid-layout-0.1.5.tgz` allow-list, LICENSE, THIRD_PARTY_NOTICES 통과. 검증 후 생성 tarball은 삭제했다.
- `npm run verify:full`: security 15/15, license, TypeScript, Vitest 49/49, build, Playwright 60 passed/17 intentional skipped 통과.
- 100-widget `chromium-resource`: 1/1 통과. Column 및 interaction 각 구간의 Documents, DOM Nodes, Event Listeners 고정과 12% transient/2% trailing final heap budget 통과.
- 최종 문서/version 정합성 변경 후 sensitive-data focused 6/6, README 3/3, TypeScript, license gate, public identity gate, `gitleaks dir --redact`, `git diff --check` 통과.

### 실패 분류 및 조치

- 최초 focused Playwright의 responsive DOM/state 불일치는 GridStack의 throttled resize 이후 실제 열을 commit하도록 수정했다. 사용자 지정 drag handle 실패는 테스트 좌표가 handle 경계를 벗어난 테스트 계약 문제로 분류하고 handle 중심 좌표로 수정했다.
- 최초 `npm run test:consumer`는 sandbox network 대기로 중단했고, 허용된 네트워크 환경에서 동일 명령이 통과했다.
- 최초 exact artifact gate는 기본 npm cache 권한 `EPERM`으로 실패했다. 홈 권한을 변경하지 않고 격리 `/tmp` cache로 동일 gate를 통과했다.

### 잔여 리스크

- 구현 및 로컬 검증 범위의 확인된 제품, 테스트 계약, 라이선스 gate 잔여 리스크는 없다.
- Firefox/Safari 검증과 breakpoint별 독립 layout persistence는 이번 0.1.5 범위 밖이며, 후자는 0.2.0 후보로 유지한다.
- Push, PR, npm stage/publish, tag, GitHub Release는 수행하지 않았다.

## 14:13 KST - PR review remediation

### 요약

- PR #12의 병합 전 독립 리뷰에서 controlled React DOM이 먼저 제거된 뒤 GridStack engine node가 남는 CRUD 회귀를 확인했다.
- 어댑터가 위젯 ID별 element registry를 소유하도록 변경하고, 분리된 element도 GridStack public `removeWidget()`으로 정리해 remove, clear, 동일 ID 재추가의 engine/state 일관성을 복구했다.
- GridStack 13이 런타임에 완전하게 갱신하지 않는 RTL과 size-to-content는 package adapter를 공개 lifecycle로 재초기화하도록 변경했다. 상속된 RTL은 초기화 전에 computed direction으로 해석하고, 전환 시 기존 `left`/`right` positioning을 정리한다.
- React state, widget ID, public handle 계약은 유지하며 `nonce`만 initialization-only로 유지했다.

### 변경 파일

- `src/components/DashboardGrid.tsx`
- `src/gridstack/adapter.ts`
- `example/src/readme-demo.tsx`
- `test/playwright/specs/dashboard-grid.spec.ts`
- `README.md`
- `docs/superpowers/specs/2026-07-23-gridstack-v13-public-api-license-design.md`
- `reports/2026-07-23.md`

### 검증

- 변경 전 focused Chromium 3건에서 engine stale ID, RTL 양방향 positioning 잔존, size-to-content class 미갱신을 각각 재현했다.
- 변경 후 focused Chromium 4/4 통과: runtime drag handle, remove/clear/same-ID re-add, inherited/runtime RTL, size-to-content.
- `npm run typecheck`: 통과.
- `npm run test:run`: Vitest 49/49 통과.
- 포트 바인딩이 허용된 환경의 `npm run verify:full`: security 15/15, license, TypeScript, Vitest 49/49, build, Playwright 63 passed/20 intentional skipped 통과.
- 100-widget `chromium-resource`: Documents, DOM Nodes, Event Listeners 고정 및 기존 heap budget 통과.
- `npm run test:consumer`: React 18 소비자 tarball 설치 및 build smoke 통과.
- 격리 `/tmp` npm cache의 `npm run verify:package-artifact`: exact `comins-grid-layout-0.1.5.tgz`, LICENSE, THIRD_PARTY_NOTICES gate 통과. 생성 tarball은 검증 후 삭제했다.
- `git diff --check`: 통과.

### 실패 분류 및 조치

- 최초 전체 게이트의 `listen EPERM 127.0.0.1:6001`은 샌드박스 포트 바인딩 실패로 분류하고 허용된 환경에서 동일 게이트를 재실행했다.
- 최초 consumer 검증은 샌드박스 네트워크 대기로 중단했고, 허용된 네트워크 환경에서 동일 명령이 통과했다.

### 잔여 리스크

- 병합을 차단한 Critical 1건과 Important 2건은 회귀 테스트와 전체 게이트로 해소했다.
- Firefox/Safari와 breakpoint별 독립 layout persistence는 기존 0.1.5 범위 밖 항목으로 유지한다.
- 수정 커밋의 원격 CI 재검증과 PR 병합은 아직 수행하지 않았다.
