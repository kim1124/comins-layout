# 2026-07-20

## Local Repository and Release Security Hardening

### Work Time

- `2026-07-20 18:32 KST`

### Summary

- Created the local `codex/security-hardening` branch from clean `main`; no remote push, PR, npm stage, publish, or remote setting change was performed.
- Pinned all GitHub Actions references to full commit SHAs and disabled persisted checkout credentials.
- Split staged publishing into an unprivileged `verify-and-pack` job and an `npm` environment-gated `stage` job; only the latter has `id-token: write` and it receives the verified package tarball through an immutable workflow artifact.
- Added weekly grouped Dependabot version updates for npm and GitHub Actions without auto-merge or release behavior.
- Added local ignore rules for environment, npm credential, private-key, and key-container files.
- Escaped consumer-controlled widget IDs with `CSS.escape()` before using them in DOM attribute selectors.
- Corrected the unreleased `0.1.2` changelog statement and documented the supported security-fix release line.

### Changed Files

- `.github/workflows/verify.yml`
- `.github/workflows/publish.yml`
- `.github/dependabot.yml`
- `.gitignore`
- `CHANGELOG.md`
- `SECURITY.md`
- `src/gridstack/adapter.ts`
- `test/vitest/adapter.test.ts`
- `test/playwright/specs/dashboard-grid.spec.ts`
- `docs/superpowers/specs/2026-07-20-security-hardening-design.md`
- `docs/superpowers/plans/2026-07-20-security-hardening.md`
- `reports/2026-07-20.md`

### Verification

- PASS: clean baseline `npm run verify:full` outside the sandbox (`5` Vitest files, `19` tests, production build, `51` Playwright tests passed, `10` intentional mobile skips).
- PASS TDD RED: `npm run test:run -- test/vitest/adapter.test.ts` failed with `findWidgetElementById is not a function` before the adapter implementation.
- PASS TDD GREEN: the same focused Vitest command passed after the minimal `CSS.escape()` implementation.
- PASS: focused Chromium regression `supports selector-significant widget IDs` (`1/1`) restored an ID containing selector-significant characters and confirmed GridStack node attachment without browser diagnostics.
- PASS: final `npm run verify:full` (`6` Vitest files, `20` tests, production build, `53` Playwright tests passed, `10` intentional mobile skips).
- PASS: the isolated `chromium-resource` project retained stable 100-widget DOM node, listener, document, and post-GC heap counters.
- PASS: `npm run test:consumer` installed `8` packages in an isolated consumer and imported the package and stylesheet exports.
- PASS: `.github/workflows/verify.yml`, `.github/workflows/publish.yml`, and `.github/dependabot.yml` parsed with Ruby Psych.
- PASS: official action tag refs were resolved directly from the official action repositories before pinning full commit SHAs.
- PASS: no mutable `@vN` action reference remains in `.github`.
- PASS: `npm --cache /tmp/comins-grid-layout-security-npm-cache pack --dry-run --json` produced `29` files for `comins-grid-layout@0.1.2`.
- PASS: an actual package tarball contained `29` files and no audited personal home path, email address, private-key marker, or common GitHub, npm, AWS, or OpenAI token pattern, including source maps.
- PASS: the current project content, excluding dependency and generated artifact directories, contained no audited personal home path, email address, private-key marker, or common provider token pattern.
- PASS: `npm audit --json` and `npm audit --omit=dev --json` both reported `0` vulnerabilities at every severity.
- PASS: `git diff --check`, untracked-file trailing-whitespace checks, action-reference review, full diff review, and final scope review.

### Environment Classification

- The first baseline browser gate attempt failed because the sandbox denied `127.0.0.1:6001` with `listen EPERM`; the identical command passed outside the sandbox.
- The first consumer smoke attempt stalled while registry network access was unavailable and was interrupted; the identical command passed with network access.
- The default npm cache remains partially root-owned, so the first pack dry-run failed with `EPERM`; package inspection passed with an isolated temporary cache. No ownership-changing or destructive command was run.
- The first YAML parser invocation used a Ruby Psych method unavailable in the installed Ruby version; the same files passed through the supported `safe_load(File.read(...))` API.

### Residual Risks

- The workflow changes and Dependabot configuration are local only. GitHub will not enforce or execute them until the branch is pushed, reviewed through a PR, passes the required `verify` check, and is merged into `main`.
- The split staged-publishing workflow has not been executed on GitHub. Its first use after merge must confirm artifact transfer, exact trusted-publisher matching, environment approval, OIDC staging, and npm 2FA approval without making assumptions from local YAML validation.
- Repository-level full-SHA enforcement should be enabled only after these pinned workflows reach `main`.
- CodeQL, Dependabot malware alerts, GitHub account 2FA, npm account identity, and current trusted-publisher settings remain remote checks or settings and were not changed in this local patch.
- Historical Git objects and already-published npm maintainer metadata remain outside this patch and require separate support or explicitly approved destructive remediation.
- The default npm cache ownership issue remains a local environment maintenance item; this work used an isolated cache instead of changing home-directory permissions.
