# 2026-07-20

## Release Resource Gate Warm-up Stabilization

### Work Time

- `2026-07-20 20:19 KST`

### Summary

- GitHub Actions release run `29734275134` failed only the isolated 100-widget resource assertion after DOM nodes, listeners, and documents remained stable.
- Moved runner-specific V8 lazy initialization outside the measured interval by increasing the pre-baseline column-cycle warm-up from one cycle to two.
- Preserved the existing heap tolerance, monotonic-growth rejection, and DOM, listener, and document assertions.

### Changed Files

- `test/playwright/specs/dashboard-grid.spec.ts`
- `reports/2026-07-20.md`

### Verification

- RED: GitHub Actions release run `29734275134` failed `staysWithinHeapBudget()` after a one-time heap increase from approximately `24.6 MB` to `27.2 MB`; DOM nodes, listeners, and documents remained unchanged.
- PASS baseline classification: the unmodified focused `chromium-resource` test passed `3/3` local repetitions, confirming that the failure is specific to the GitHub runner initialization timing.
- PASS: the focused post-change `chromium-resource` test passed `3/3` repetitions with stable DOM node, listener, document, and post-GC heap counters.
- PASS: `npm run verify:full` completed with all `19` Vitest tests and `51` Playwright tests passing, `10` intentional mobile cases skipped, and a successful production build.
- PENDING: required GitHub `verify` check.

### Residual Risks

- Absolute resource counters vary by runner. Acceptance continues to rely on same-session post-GC stability and unchanged DOM, listener, and document counts.
