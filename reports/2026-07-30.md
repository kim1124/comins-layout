# 2026-07-30

## 00:13 KST - 0.1.6 로컬 릴리스 준비

### 요약

- `origin/main`의 npm service-identity gate 병합 커밋
  `c9ef97250dadf1de97f84991e53646fabdcf84cf`에서
  `codex/release-0.1.6` 격리 작업트리를 생성했다.
- 패키지와 lockfile 루트 버전, 지원 버전 표, CHANGELOG를 `0.1.6`으로
  정렬했다.
- `0.1.6`은 fail-closed npm service-identity gate, React Router `8.3.0`과
  PostCSS `8.5.24` 개발 도구 갱신, Contract v1.4 적용을 포함한다.
- runtime dependency, 공개 API·타입, CSS, GridStack 동작은 변경하지 않았다.
- release metadata 변경의 라이선스 영향은 `N/A`이며, 실제 릴리스 후보이므로
  기존 라이선스 및 exact-artifact gate는 모두 실행했다.

### 변경 파일

- `docs/superpowers/plans/2026-07-30-release-0.1.6.md`
- `test/security/sensitive-data-gates.node.mjs`
- `package.json`
- `package-lock.json`
- `SECURITY.md`
- `CHANGELOG.md`
- `reports/2026-07-30.md`

### Provider 준비 상태

- `Verify npm identity` current-owner 실행 `30463234132`:
  `main`의 base SHA에서 `completed/success`.
- `COMINS_NPM_PUBLIC_NAME`, `COMINS_NPM_PUBLIC_EMAIL` repository secret:
  두 항목 모두 존재함을 값 없이 확인했다.
- 실제 npm 사용자명, 서비스 이메일, 복구 주소, credential, OTP, token 및
  값 기반 fingerprint는 조회·출력·기록하지 않았다.
- 관리자 준비 승인 이후 npm 이메일, 소유자, trusted publisher 설정은
  exact-version closure까지 동결하는 조건으로 진행한다.

### 검증

- 격리 작업트리 baseline `npm run verify`: 통과.
  - security `25/25`
  - license gate 통과
  - TypeScript 통과
  - Vitest `49/49`
  - Vite 및 declaration build 통과
- TDD RED:
  `node --test test/security/sensitive-data-gates.node.mjs`는 저장소가
  `0.1.5`를 선언해 `5/6` 통과, `1/6` 실패했다.
- TDD GREEN:
  동일 focused test는 `0.1.6` 계약 적용 후 `6/6` 통과했다.
- `npm install --package-lock-only --ignore-scripts`: 통과. dependency
  resolution 변화 없이 lockfile 루트 버전 2곳만 변경됐다.
- `npm run verify:full`: 통과.
  - security `25/25`
  - license, TypeScript, Vitest `49/49`, build 통과
  - Playwright `63 passed`, `20 skipped`
  - 100 Widget 반복 column/resource 안정성 검사 통과, 실행 시간 `15.4s`
- `npm run --silent verify:package-artifact`: exact
  `comins-grid-layout-0.1.6.tgz` 생성, files allow-list, `LICENSE`,
  `THIRD_PARTY_NOTICES.md` 통과.
- 동일 exact archive 추출본의 Gitleaks `8.30.1` redacted 검사: 통과.
- 동일 exact archive의 React 18 consumer smoke:
  - 첫 실행은 sandbox DNS 제한으로 npm Registry가 `ENOTFOUND`인 실행 환경
    문제를 확인하고 중단했다.
  - 코드 또는 archive를 변경하지 않고 Registry 접근을 허용한 재실행에서
    package import와 `styles.css` export가 통과했다.
- workflow YAML parse: `verify.yml`, `publish.yml`,
  `verify-npm-identity.yml` 3개 모두 통과.
  - 최초 계획 명령의 `aliases` 키워드는 로컬 Ruby `2.6`에서 지원되지 않아
    제품 실패와 분리했고, YAML alias를 사용하지 않는 현재 workflow에 맞춰
    `YAML.load_file(file)`로 계획 명령을 수정했다.
- `git diff --check`: 통과.

### 잔여 이슈 및 승인 경계

- push, pull request, merge, GitHub Actions staging, npm staged package 2FA
  승인, 공개 배포, tag, GitHub Release 및 Provider 설정 변경은 수행하지
  않았다.
- 원격 push와 pull request 생성은 별도 승인 후 수행한다.
- 병합은 required check 결과와 diff 검토 후 별도 승인 대상이다.
- staging workflow dispatch와 npm 2FA 승인은 각각 별도 승인 대상이다.
- 공개 후 `0.1.6` exact-version identity, provenance, integrity, public
  archive, Gitleaks 및 React 18 consumer closure가 모두 통과해야 릴리스를
  종료할 수 있다.
- `0.1.0`부터 `0.1.5`까지의 publication-time metadata,
  `0.1.0` legacy artifact pattern, npm Support/privacy 처리, profile
  surface 및 deprecation은 이번 릴리스와 분리된 Provider 리스크다.
