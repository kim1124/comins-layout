# 2026-07-29

## 18:09 KST - Comins npm 서비스 계정 검사 게이트

### 요약

- Grid Layout 관리 지침을 Comins Contract v1.4로 동기화하고, 라이선스
  선분류와 실제 공개 배포 조건을 서비스 계정 설계·계획에 반영했다.
- 현재 npm 관리자와 정확한 공개 버전의 GitHub trusted publisher,
  approver, author, contributors를 값 없이 검증하는 fail-closed Node
  검사기를 추가했다.
- publish workflow는 제품 게이트 전과 `npm stage publish` 직전에 현재
  관리자 신원을 검사한다.
- 모바일에서 실행할 수 있는 읽기 전용 current-owner/exact-version
  workflow를 추가했다.
- 기대 신원은 공개 서비스 메타데이터지만 GitHub configuration variables가
  기본적으로 로그에서 마스킹되지 않으므로 repository secrets를 사용하도록
  확정했다. 실제 값은 저장소·테스트·로그·보고서에 기록하지 않았다.

### 변경 파일

- `AGENTS.md`
- `docs/superpowers/specs/2026-07-27-comins-npm-service-identity-design.md`
- `docs/superpowers/plans/2026-07-27-comins-npm-service-identity.md`
- `scripts/check-npm-registry-identity.mjs`
- `test/security/npm-registry-identity.node.mjs`
- `test/security/sensitive-data-gates.node.mjs`
- `package.json`
- `.github/workflows/publish.yml`
- `.github/workflows/verify-npm-identity.yml`
- `reports/2026-07-29.md`

기존 사용자 파일 `reports/2026-07-27.md`는 변경하지 않았다.

### 공통 정책

- Governance 별도 worktree와 `codex/npm-service-identity-v14` 브랜치에서
  Contract v1.4 서비스 계정·배포 경계를 구현했다.
- Governance 로컬 커밋: `3e7ada2` (`docs: define Comins npm service identity`).
- `COMINS_CONTRACT.md`, 관리 모듈 템플릿, `OSS_LICENSE_POLICY.md`는 변경하지
  않았다.

### 검증

- 라이선스 영향: 신규 의존성·복사/수정 제3자 코드·생성 자산·번들·package
  contents 변경이 없어 구현 단계 `N/A`; `npm run verify`의 기존 라이선스
  gate는 통과했다.
- TDD RED: 검사기 파일 부재, package script 부재, workflow 부재, 잘못된
  기대 이메일 허용, unmasked variables 및 workflow 순서 계약 불일치를
  각각 확인했다.
- npm identity focused test: 8/8 통과.
- sensitive-data focused test: 6/6 통과.
- 전체 security test: 23/23 통과.
- workflow YAML parse: 3/3 통과.
- `npm run verify`: 통과.
  - Security 23/23
  - License gate 통과
  - TypeScript typecheck 통과
  - Vitest 49/49
  - Vite 및 TypeScript build 통과
- 변경 파일 Gitleaks redacted 검사: 9/9 누출 없음.
- `git diff --check`: 통과.
- 독립 코드 리뷰: 최초 Important 3건, Minor 1건을 수정한 뒤 재검토에서
  모두 해소, 신규 Critical/Important 없음, Ready.

### 잔여 이슈

- Comins 메일함 준비 완료 여부, npm 계정 이메일 변경·검증, GitHub
  repository secrets 설정은 아직 확인하거나 수행하지 않았다.
- live current-owner workflow와 다음 실제 공개 버전의 exact-version
  workflow는 Provider 설정 및 원격 반영 전까지 실행할 수 없다.
- `verify:full`은 GridStack·브라우저 동작 변경이 없어 실행하지 않았다.
  다음 실제 공개 배포는 기존 full browser 및 exact-artifact gate를
  별도로 실행해야 한다.
- 공개 버전 `0.1.0`부터 `0.1.5`까지의 과거 registry metadata와 별도
  legacy artifact risk는 변경되지 않는다.
- npm이 trusted-publisher metadata shape를 변경하면 검사기는 fail closed
  된다. 새 shape를 값 없이 재검토하기 전에는 릴리스를 진행하지 않는다.
- push, PR, merge, tag, GitHub Release, npm stage/publish 및 Provider 설정
  변경은 수행하지 않았다.
