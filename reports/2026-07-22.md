# 2026-07-22 Local Integration And Residual-Risk Audit

## 작업 일시

- 2026-07-22 00:38 KST

## 요약

- 로컬 `main`을 최신 `origin/main`으로 fast-forward한 뒤 `codex/privacy-baseline-simple-grid`을 병합했다.
- 양쪽에서 추가된 `reports/2026-07-21.md` 충돌은 release/privacy 이력과 Contract v1.2 보안 이력을 모두 보존해 해결했다.
- 병합된 `main`에서 전체 제품·보안·브라우저·소비자·package artifact gate를 재검증했다.
- 대체 예정 보안 브랜치에서 확인된 selector-significant widget ID 결함을 `CSS.escape` 기반 최소 수정과 회귀 테스트로 이식했다.

## 변경 파일

- `src/gridstack/adapter.ts`
- `test/vitest/adapter.test.ts`
- `test/playwright/specs/dashboard-grid.spec.ts`
- `reports/2026-07-22.md`

## 검증

- selector-significant widget ID 회귀 테스트: 수정 전 의도한 실패 확인 후 1/1 통과
- selector-significant widget ID 브라우저 복원 회귀 테스트: Chromium과 mobile Chrome 통과
- `npm run verify:full`: 보안 7/7, Vitest 24/24, TypeScript, production build, Playwright 53 통과·10 mobile skip
- `npm run test:consumer`: 격리 소비자 설치 및 import 통과
- exact `comins-grid-layout-0.1.2.tgz` file-list 및 추출 Gitleaks 검사: 통과
- `origin/main..main` raw identity, Gitleaks, diff 검사: 통과
- tracked pre-commit 훅 실행: 통과
- 생성 package archive와 임시 추출 디렉터리 제거: 완료
- worktree 상태: clean

## 원격 및 provider 감사

- Private Vulnerability Reporting, Secret Protection, Push Protection, Dependabot alerts는 활성 상태다.
- 활성 `main-protection` ruleset은 삭제 제한, force-push 차단, 최신 branch 요구, `verify`를 필수 check로 사용한다.
- CodeQL default setup은 비활성이나 현재 최소 Contract의 필수 gate는 아니다.
- Support 정리 대상이던 과거 Git commit은 더 이상 GitHub에서 조회되지 않는다.
- npm `0.1.0`과 `0.1.1`은 아직 공개이고 `0.1.2`가 latest다.
- npm maintainer metadata는 승인된 GitHub noreply identity 형태가 아니다.
- npm Support에 보낸 2026-07-20 privacy unpublish 후속 요청 이후 회신은 확인되지 않았다.
- 대체된 기존 security PR은 원격에 열린 상태다.

## 잔여 조치

- npm Support에 `0.1.0`·`0.1.1` privacy unpublish 진행 상태를 재문의해야 한다.
- 대체된 기존 security PR의 종료는 별도 원격 변경 승인이 필요하다.
- npm 계정 maintainer email 변경은 계정 전체에 영향을 주므로 목표 공개 identity를 확정한 별도 승인이 필요하다.
- 새 로컬 `main`은 원격에 push하지 않았다.

## GitHub Service Committer Compatibility — 01:32 KST

### 요약

- Dependabot 커밋의 GitHub service committer가 공통 public-identity gate에서 차단되는 문제를 재현하고 수정했다.
- 허용 범위는 GitHub가 사용하는 정확한 service name/address 조합으로 제한했다.
- CI의 일반 `verify`가 runner의 로컬 Git identity를 검사해 실패하던 계약을 제거했다. 로컬 identity는 Git hook에서, PR commit range는 별도 `Sensitive data` job에서 계속 검사한다.

### 변경 파일

- `scripts/check-public-identities.mjs`
- `test/security/sensitive-data-gates.node.mjs`
- `package.json`

### 검증

- 회귀 테스트 수정 전 실패 및 수정 후 통과 확인
- CI 실패를 재현한 test-contract 수정의 red/green 확인
- `npm run verify:full`: 보안 8/8, Vitest 24/24, TypeScript, production build, Playwright 53 통과·10 mobile skip
- `npm run test:consumer`: 격리 소비자 설치 및 import 통과

### 잔여 리스크

- checker 동작 범위의 잔여 리스크는 확인되지 않았다. npm 과거 버전과 maintainer metadata 정리는 별도 운영 항목이다.

## Contract v1.2 필수 검사 및 0.1.3 릴리스 준비 — 09:45 KST

### 요약

- governance Contract v1.2 managed block과 저장소 `AGENTS.md`의 byte 일치를 재확인했다.
- GitHub `main-protection` ruleset에 기존 `Sensitive data` job을 `verify`와 함께 필수 상태 검사로 추가했다.
- ruleset 변경 전후를 대조해 삭제 제한, force-push 차단, PR 요구, strict update, bypass actor 및 기타 조건이 유지됨을 확인했다.
- selector-significant widget ID 복원 수정과 Contract v1.2 보안 gate를 포함하는 안정 patch `0.1.3` 릴리스 메타데이터를 준비했다.

### 변경 파일

- `docs/superpowers/specs/2026-07-22-contract-v1.2-release-0.1.3-design.md`
- `docs/superpowers/plans/2026-07-22-contract-v1.2-release-0.1.3.md`
- `package.json`
- `package-lock.json`
- `CHANGELOG.md`
- `reports/2026-07-22.md`

### 검증

- `npm run test:security`: 8/8 통과
- `npm run verify:full`: 보안 8/8, Vitest 24/24, TypeScript, production build, Playwright 53 통과·10 mobile desktop-input 계약 skip
- Playwright 100-widget resource project: 반복 column cycle과 interaction 이후 DOM node·listener 고정, post-GC heap 비단조 상태로 통과
- `npm run test:consumer`: 격리 React 18 소비자 설치, package import, stylesheet export 해석 통과
- `npm run verify:package-artifact`: exact `comins-grid-layout-0.1.3.tgz` allow-list 검사 통과
- 추출 package Gitleaks `8.30.1` redacted scan: 통과
- `.github/workflows/verify.yml`, `.github/workflows/publish.yml` Ruby Psych parse: 통과
- Contract v1.2 managed guidance byte 비교, `git diff --check`, 생성 archive 및 임시 디렉터리 정리: 통과
- 최초 `verify:full`의 Playwright server bind `EPERM`과 최초 소비자 smoke의 registry 대기는 실행환경 문제로 분류했고, 허용된 환경에서 동일 명령을 재실행해 통과했다.

### 잔여 조치

- 릴리스 PR에서 새 필수 검사 `Sensitive data`와 기존 `verify`가 정확한 head SHA에 통과해야 한다.
- 병합된 `main`의 정확한 SHA로 trusted staged-publishing workflow를 실행해야 한다.
- staged artifact를 확인한 뒤 maintainer가 npm UI에서 2FA 승인해야 하며, 이후 registry `latest`, provenance, integrity, public tarball 및 public consumer 검증이 필요하다.
- 과거 npm 버전과 account maintainer metadata 정리는 이번 릴리스 범위 밖의 별도 운영 리스크다.
