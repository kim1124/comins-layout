# 2026-08-24 작업 리포트

## 작업 일시

- 2026-08-24 18:42 KST

## 요약

- Comins Contract v1.7의 affected-check 원칙에 맞춰 Playwright 실행 행렬과 E2E 스펙을 기능 계약 중심으로 정리했다.
- 전체 실행 수를 97건에서 64건으로 줄였다. Chromium 54건, Firefox 대표 parity 6건, Mobile Chrome touch 3건, Chromium resource 1건이다.
- 동일 계약의 Firefox 반복 19건, 중복 종합/CRUD/상태 복원 시나리오, 정적 타이포그래피·아이콘·색상 검사, Vitest와 중복된 metadata 반복 입력을 제거했다.
- Safari/WebKit은 자동 지원 계약에서 제외하고 별도 소비자 검증 경계로 유지했다.
- 자동 재시도는 사용하지 않으며 실패 trace만 보존한다.

## 변경 파일

- `playwright.config.ts`
- `docs/04-verification-strategy.md`
- `test/playwright/README.md`
- `test/playwright/specs/README.md`
- `test/playwright/specs/dashboard-grid.spec.ts`
- `test/playwright/specs/docs-playground-routing.spec.ts`
- `test/playwright/specs/playground.spec.ts`
- `test/playwright/specs/example.spec.ts` 삭제
- `test/playwright/specs/visual-typography.spec.ts` 삭제
- `test/vitest/playwright-project-policy.test.ts`

## 수행한 검증

- `npm run lint`: 통과
- `npm run test:run -- test/vitest/playwright-project-policy.test.ts test/vitest/playground-state-snapshot.test.ts`: 16/16 통과
- `npm run test:e2e`: 64/64 통과, 자동 재시도 없음, 45.3초
- `npm run verify`: 보안 25/25, Vitest 107/107, 라이선스, TypeScript, build 통과

## 결과

- 제거한 테스트가 담당하던 기능 계약은 유지된 Chromium 시나리오 또는 Vitest 단위 계약으로 대체되어 있다.
- Firefox는 드래그/리사이즈 callback 순서, 외부 드롭, double-click, 컬럼 전환 중 resize, 브라우저 경계 이탈 drag/resize만 대표 parity로 검증한다.
- Mobile touch와 100-widget resource gate는 다른 프로젝트로 대체할 수 없어 유지했다.

## 잔여 이슈

- Firefox 검증은 전체 Chromium 스펙 복제가 아니라 엔진 차이에 민감한 대표 시나리오만 다룬다.
- Safari/macOS/iOS는 자동 검증 범위가 아니며 별도 소비자 검증이 필요하다.
- `main` merge와 publish는 수행하지 않는다. 검증 변경은 별도 브랜치와 PR로 원격 반영한다.
