# 2026-08-24 PR and E2E stability review

## Summary

- Confirmed the module-managed guidance and Governance remote `main` both use Contract v1.6 at `562cb965951a5e9c4abe97f535eadf3755a3f678`.
- Reproduced the Firefox localization flake: `readDashboardGeometry` counted GridStack's transient drag placeholder as a seventh widget, so the same assertion could fail or pass without proving a committed layout change.
- Made dashboard geometry reads atomic and limited them to live widgets with `data-widget-id`.
- Added GridStack readiness, viewport scrolling, activation, pointer release, and interaction-end handling to shared drag and resize helpers while preserving disabled-widget and external-drop contracts.
- Removed the duplicate saved-snapshot interaction from the localization scenario. The canonical save/locale/restore contract remains in `playground-layout-examples.spec.ts`; localization still verifies copy, route, and no grid remount.
- Added an explicit resize-active condition to the WebKit controlled-cache scenario.
- Made PR Playwright jobs fail on flaky retries and extended browser evidence retention from 3 to 7 days.

## Changed files

- `.github/workflows/verify.yml`
- `test/playwright/helpers/dashboard-interactions.ts`
- `test/playwright/specs/dashboard-grid.spec.ts`
- `test/playwright/specs/playground-layout-examples.spec.ts`
- `test/playwright/specs/playground-localization.spec.ts`
- `test/vitest/playwright-project-policy.test.ts`
- `reports/2026-08-24.md`

## Verification

- TDD RED: the new placeholder regression failed in Firefox on the initial attempt and retry because geometry contained a seventh `{ id: null }` entry.
- Policy RED: `playwright-project-policy.test.ts` failed before the workflow enabled flaky-test failure and 7-day retention.
- Focused GREEN:
  - Firefox placeholder regression: 1/1 passed.
  - Firefox localized Layout scenario repeated 10 times: 10/10 passed with flaky failures treated as errors.
  - Firefox Layout examples: 11/11 passed.
  - Firefox Advanced examples: 20/20 passed; the external-drop scenario separately passed 10/10.
  - WebKit controlled-cache resize scenario repeated 10 times: 10/10 passed.
  - Playwright policy Vitest: 4/4 passed.
- Package baseline `npm run verify`: security 25/25, license check, typecheck, Vitest 18 files and 163/163 tests, and build passed.
- `git diff --check`: passed before the report was added and is rerun at handoff.

## PR and guidance findings

- The only open pull request is #26. It is mergeable but blocked by its previous WebKit failure; there is no second open PR to close as redundant.
- PR #26 is large (56 commits, 59 files, 10,397 additions, 1,958 deletions). Splitting it after this amount of same-branch verification would discard useful evidence, so the recommended closure is one focused update and a single affected CI rerun before new feature work.
- The Comins instruction inventory reported zero repository-guidance findings for Governance and grid-layout. The extra instruction cost observed in this run came from activated general-purpose skills, not the managed Contract v1.6 chain.

## Residual risks

- At the end of the initial review, no commit, push, pull request update, merge, publish, or deployment had been performed.
- At the end of the initial review, GitHub had not evaluated the local changes. PR #26 remained blocked pending a focused update and affected CI.
- The Governance repository contains an unrelated untracked `reports/2026-07-27.md`; it was not modified.

## Follow-up loop, branch, and PR audit

- No Playwright, Vite, or GitHub Actions watcher remains in the grid-layout loop. A watcher observed during the process belonged to a separate Sortable worktree and exited automatically when its cancelled run completed.
- The active guidance inventory still reports zero repository findings. The current external finishing and fresh-verification skill chain adds 14,535 bytes and three trigger findings; no extra subagent, planning, browser, or CI-polling loop was started.
- Recent PR #26 runs do not show one scenario failing on every run. The latest two failures were different WebKit scenarios, while the prior run completed successfully. The identified Firefox retry-only localization scenario and the latest WebKit controlled-cache scenario are covered by the focused repeat evidence above.
- PR #25 is closed and replaced by #29, but its owner comment explicitly says to preserve the branch. The clean `codex/publish-chromium-only` worktree and remote branch were therefore not removed.
- The detached `codex-task2-base-red` worktree contains modified and untracked test files. It is user-owned unfinished state and was preserved.
- PR #26 remains the only open PR and is the expected target for this focused stability update.
