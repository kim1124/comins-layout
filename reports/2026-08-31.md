# 2026-08-31 작업 보고

## 00:10 KST - 0.2.2 Playground 후속 수정 및 메뉴 중복 검토

### 요약

- 위젯 이동 아이콘을 4방향 화살표 `Move`, 리사이즈 아이콘을 대각선 양방향 화살표 `MoveDiagonal2`로 교체했다.
- 번호 위젯의 기본 geometry를 W3/H2로 변경하고 12컬럼당 4개씩 배치했다.
- Widget Events에 이벤트명/기능 설명 표를 추가했다. 요청한 예제 이벤트명 `onDblClickTitle`은 실제 public prop `onTitleDoubleClick`에서 수신한다고 함께 명시해 존재하지 않는 API로 오인되지 않도록 했다.
- Safe Public Handlers / Methods에 8개 공개 메서드와 기능 설명 표를 추가했다.
- 공개 Playground 메뉴에서 빠져 있던 외부 휴지통 드롭 삭제 예제를 `/examples/advanced/external-drop-trash`로 복원했다. package는 typed drop event만 전달하고 consumer가 `targetId === "trash"` 확인 후 `removeWidget`을 호출하는 제어 상태 계약을 유지한다.
- README와 영문·국문 사용자 가이드에서 다중 Grid 전송과 외부 휴지통 드롭의 route 책임을 분리했다.

### 원인 및 기능 경계

- A GridStack 위젯을 B GridStack으로 이동·복사하는 기능은 이미 `/examples/advanced/multi-grid/horizontal`과 vertical route의 `TransferPlayground`에 구현되어 있다. `gridId`, `acceptExternalWidgets`, `gridTransferMode`, `onWidgetDropRequest`, `transferDashboardWidget`의 제어 상태 경로를 사용하며 실제 pointer drag와 버튼 대안을 제공한다.
- Nested Grid는 Root/Child/Grandchild React 상태 소유권과 중첩 interaction 경계를 설명하는 예제다. 같은 Grid 내부 layout 이동과 A→B Grid 전송은 서로 다른 계약이므로, Nested 설명과 컨트롤 영역에서 다중 Grid 전송 예제로 연결했다.
- 휴지통 드롭 기능은 hidden `/__test__/advanced` fixture와 `/readme-demo?feature=external-drop`에는 남아 있었지만 0.2.1 메뉴 통합 후 전용 public Playground route가 없었다. 기능 제거가 아니라 공개 route 누락이 원인이었다.

### 메뉴 중복 검토

| 후보 | 확인 결과 | 권고 |
|---|---|---|
| 다중 Grid - 가로 / 세로 | 같은 `TransferPlayground`에 `orientation`과 title만 다르다. 전송 API·상태·컨트롤은 동일하다. | 하나의 route와 방향 토글로 통합하고 기존 두 route는 compatibility redirect로 유지하는 것을 권고한다. |
| Nested Grid - Basic / 고급 | 같은 `NestedPlayground`에서 depth 2/3만 다르다. 재귀 깊이 외의 API 계약은 동일하다. | 하나의 Nested Composition route에서 depth 컨트롤로 통합하는 것을 권고한다. |
| Nested Grid - 제약 | `acceptExternalWidgets` predicate 선언 위치는 보여주지만 현재 페이지 안에 외부 source가 없어 승인/거부를 직접 실행할 수 없다. | palette 또는 sibling Grid source를 추가하지 않을 경우 메뉴 제거를 권고한다. |
| Responsive 3종 | `columnWidth`, explicit breakpoints, `layout: "none"`의 geometry 계약이 각각 다르다. | 유지한다. |
| Static Grid / 레이아웃 잠금 | engine `staticGrid` 전환과 `movable`/`resizable` prop 전환으로 동작 계층이 다르다. | 유지한다. |
| Widget Events / Layout Events | pointer lifecycle과 semantic state mutation event로 관찰 대상이 다르다. | 유지한다. |

메뉴 삭제·redirect는 route compatibility와 문서 링크에 영향을 주므로 이번 검토에서는 수행하지 않았다.

### 변경 파일

- Widget UI·fixture: `example/src/playground/components/ExampleDashboard.tsx`, `example/src/playground/fixtures.ts`
- 안내·표: `example/src/playground/components/ExampleGuide.tsx`, `example/src/playground/WidgetExamples.tsx`, `example/src/playground/AdvancedExamples.tsx`
- 신규 route·메뉴: `example/src/main.tsx`, `example/src/playground/routes.ts`
- 스타일: `example/src/styles.css`
- 문서: `README.md`, `CHANGELOG.md`, `docs/{user,ko}/{08-palette-and-grid-transfer,14-playground}.md`
- 회귀: `test/playwright/specs/{playground,docs-playground-routing}.spec.ts`

### 수행한 검증

- 변경 전 회귀 RED 4/4 확인:
  - public method/event table 부재
  - Widget 기본 W2
  - 기존 `Scaling`, `GripVertical` 아이콘
  - 휴지통 public route 부재
- focused Chromium: 4/4 통과
- 휴지통 삭제 후 locale 상태 전환 focused Chromium: 1/1 통과
- 영향 범위 Chromium: 60/60 통과
  - 28개 public route, 한·영문, 메뉴 routing
  - Widget W3/H2, 아이콘, 이벤트/메서드 표
  - Nested 이동·리사이즈, 실제 Grid A→B 전송, 휴지통 드롭 삭제
- Firefox 대표 계약: 2/2 통과
  - 타이틀 이벤트 lifecycle
  - 실제 Grid A→B pointer 이동
- `npm run verify`: 통과
  - security 25/25
  - license, TypeScript 통과
  - Vitest 19 files / 180 tests
  - production build 통과
- headed Chromium 시각 확인:
  - W3 카드 4열 배치, 4방향 이동·대각선 리사이즈 아이콘, W/H 표 전체 노출
  - 이벤트 핸들러 표와 코드 영역 가로 잘림 없음
  - 휴지통 컨트롤과 안내 영역 정상 노출
  - console errors 0, warnings 0
- `git diff --check`: 통과

## 00:36 KST - 커밋 준비 최종 검토

- 최종 diff 검토 findings: 0
- active README·영문/국문 사용자 가이드에는 제거된 submenu route가 남아 있지 않다. 이전 세 경로는 compatibility map과 redirect 회귀 테스트에만 유지된다.
- `src/**`, `package.json`, `package-lock.json`, public API, dependency에는 변경이 없다.
- 의도하지 않은 Playwright 캡처, test result, build 산출물은 Git 변경 목록에 포함되지 않았다.
- 변경 파일은 0.2.2 Playground UI·가이드·route·회귀 테스트·작업 보고 범위로 한정된다.
- `git diff --check`: 통과

### 잔여 리스크

- 중복 메뉴 통합과 Nested Constraints의 실행 가능한 source 추가 여부는 제품 메뉴 변경 승인 후 결정해야 한다.
- package public API와 `src/**` runtime은 변경하지 않았다. `onDblClickTitle`은 Playground가 출력하는 예제 이벤트명이며 실제 public prop은 계속 `onTitleDoubleClick`이다.
- `npm run verify:full`, commit, push, PR, merge, npm publish는 수행하지 않았다.
- 배포 완료 전까지 `http://127.0.0.1:6001` Playground를 유지한다.

## 00:20 KST - 중복 Playground 메뉴 통합

### 요약

- `다중 Grid - 가로/세로` 메뉴를 `다중 Grid 전송` 하나로 통합하고, 페이지 안에서 가로/세로 배치를 전환하도록 변경했다.
- `Nested Grid - Basic/고급` 메뉴를 `Nested Grid - 구성` 하나로 통합하고, 페이지 안에서 2단계/3단계 구성을 전환하도록 변경했다.
- 실행 가능한 외부 source가 없어 제약 동작을 증명하지 못하던 `Nested Grid - 제약` 메뉴를 제거했다. predicate 승인/거부는 실제 팔레트와 sibling Grid source가 있는 다중 Grid 전송 예제에서 확인한다.
- 기존 vertical, nested advanced, nested constraints URL은 북마크와 문서 호환성을 위해 canonical route로 redirect한다.

### 호환 경로

| 기존 경로 | canonical 경로 |
|---|---|
| `/examples/advanced/multi-grid/vertical` | `/examples/advanced/multi-grid/horizontal` |
| `/examples/advanced/nested/advanced` | `/examples/advanced/nested/basic` |
| `/examples/advanced/nested/constraints` | `/examples/advanced/multi-grid/horizontal` |

### 검증

- 구현 전 focused Chromium RED: 4/4 실패 확인
  - 통합 메뉴 수, legacy redirect, Nested depth control, multi-grid orientation control 부재
- 구현 후 focused Chromium: 4/4 통과
- 영향 범위 Chromium: 58/58 통과
  - 25개 canonical route와 3개 legacy redirect
  - Nested 2/3단계 전환과 named Grid 소유권
  - 다중 Grid 가로/세로 전환, 실제 Grid A→B pointer 이동·복사·거부
  - Widget/레이아웃/Nested 이동·리사이즈, 휴지통 삭제, 문서 shell 및 locale
- Firefox 대표 계약: 2/2 통과
  - 타이틀 이벤트 lifecycle
  - 실제 Grid A→B pointer 이동
- `npm run verify`: 통과
  - security 25/25
  - license, TypeScript 통과
  - Vitest 19 files / 180 tests
  - production build 통과
- 브라우저 시각 및 접근성 구조 확인:
  - 단일 메뉴의 가로/세로 배치 버튼과 Nested 2/3단계 버튼 노출
  - 3단계 전환 시 Grandchild Grid 구성
  - console errors 0, warnings 0
- 최종 서버 확인에서 Nested 깊이 전환 시 일반 console이 아닌 `window.error`로 전달되는 ResizeObserver loop를 추가 발견했다. 깊이 변경 시 관찰 중인 기존 Nested 트리에 Grandchild Grid를 삽입하지 않고 해당 Grid 렌더 트리만 재마운트하도록 수정했으며, 2→3→2 전환의 `window.error` 회귀를 추가했다.
- `git diff --check`: 통과
