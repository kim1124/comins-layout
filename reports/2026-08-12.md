# 2026-08-12 — Playground docs and localization restoration

## Work record

- Final verification time: `2026-08-12 22:34 KST`
- Approved goal: restore the integrated DocsShell Playground, add complete
  Korean/English docs and example localization without resetting live state,
  remove the obsolete standalone shell, and close the local branch with fresh
  package, browser-engine, and visual evidence.
- Boundaries: no package `src/` or public API change, no dependency or package
  version change, no route removal/addition, and no push, PR, merge, publish,
  tag, Release, or other remote write.

## Changed surfaces

- Added the approved design and implementation plan under
  `docs/superpowers/`.
- Added the example-only locale provider, messages, resolved docs/API content,
  search copy, and localized Playground copy under `example/src/i18n/`,
  `example/src/docs/`, and `example/src/playground/`.
- Integrated the existing Widget, Layout, and Advanced demos into `DocsShell`,
  kept `RouteLifecycleBoundary`, and aligned example-only styling.
- Added/updated focused Vitest and Playwright behavior contracts under `test/`.
- Added Playground startup and current route guidance to `README.md`.
- Deleted the tracked, unused
  `example/src/playground/PlaygroundShell.tsx`; removed its exact
  `.playground-shell`, `.playground-nav`, and `.playground-main` CSS remnants
  and removed obsolete absence assertions from the route behavior spec.
- Updated this report in place; generated screenshots and `.playwright-cli/`
  browser state are not tracked.

## Public surface invariant

- `git diff --exit-code origin/main -- src/`: exit `0`, so the library `src/`
  tree is unchanged from `origin/main`.
- `npm run build`: PASS after final source cleanup.
- `package.json` and `package-lock.json` are unchanged on this branch; the
  example-only locale code is not included in the exported package surface.

## Task-by-task TDD evidence

The evidence below is transcribed from the ignored SDD implementer reports for
Tasks 1–6. Commands and results are historical per-task evidence; the separate
final-gate section records the fresh Task 7 reruns.

### Task 1 — locale domain and Provider

- RED: `npm run test:run -- test/vitest/playground-locale.test.ts` failed before
  implementation because `example/src/i18n/playground-locale` did not exist.
- GREEN: the same command passed `2/2`; `npm run lint` also passed.
- The unit contract covers normalization and complete-copy resolution. Browser
  storage failure, `<html lang>`, and persistence behavior moved to Task 2 E2E.

### Task 2 — localized docs/API/search

- Authoritative RED at base `2c4a4ec`:
  `COMINS_GRID_LAYOUT_PORT=6010 npm run test:e2e -- test/playwright/specs/task-2-base-red.spec.ts --project=chromium --workers=1`
  failed after waiting for the missing locale toggle.
- Initial GREEN on isolated port `6008`: localization spec `7 passed`; docs
  routing regression `8 passed`; locale unit `2 passed`; TypeScript passed.
- Review-fix GREEN on port `6009`: localization spec `9 passed`, including
  locale-neutral API notation, stable docs example DOM identity, and readme-demo
  storage invariance; TypeScript passed.
- The earlier default-port failure was excluded because another worktree server
  could be reused; only isolated-port evidence is retained.

### Task 3 — integrated DocsShell and live examples

- The initial integrated-shell RED was not durably preserved after implementer
  takeover and is not claimed as independently verified.
- Fresh GREEN: focused routing/visual Chromium `9 passed`; final Chromium
  desktop `77 passed / 3 intentional mobile-touch skips`; lint and diff checks
  passed.
- Review RED: visual geometry failed because the desktop locale control bottom
  was `110.796875` while the header containment limit was `77`.
- Review-fix GREEN: the third desktop topnav column kept brand, search, and
  locale controls inside the header; focused Chromium `9 passed`, desktop
  Chromium `77 passed / 3 skips`, and mobile visual `1 passed`.

### Task 4 — shared Playground localization and dialog state

- RED: the Chromium localization spec reported `9 passed / 4 failed`; the
  backdrop blocked required locale interaction, fixture copy stayed Korean,
  and localized Layout JSON labels were absent.
- Initial GREEN: localization `13 passed`; Widget behavior `8 passed`; affected
  dashboard regressions `3 passed`; full gate then passed with Playwright
  `302 passed / 35 policy skips`.
- Review REDs reproduced topnav/modal stacking, Advanced fixture presentation,
  and locale-dependent dialog draft reset.
- Review-fix GREEN: edit-draft and Advanced focused tests passed, localization
  `15 passed`, Widget/Advanced regressions `17 passed`, and the final full gate
  passed with Playwright `310 passed / 35 policy skips`.

### Task 5 — Widget Playground localization

- RED: focused Widget localization reported `4 passed / 1 failed` because the
  English `Lock movement` control did not exist.
- GREEN: focused Widget localization `5 passed`; Widget/drag/resize regressions
  `32 passed / 3 mobile-only skips`; full gate passed.
- Fix round 1 RED reproduced prefix/prototype title collisions and untranslated
  generated descriptions (`5 passed / 4 failed`); GREEN was `9 passed`.
- Fix round 2 RED reproduced an unhandled prototype-like restored presentation
  key (`1 failed`); GREEN was `1 passed`, followed by Widget localization
  `9 passed`, Advanced regressions `10 passed`, lint/diff checks, and the
  369-test full multi-browser gate.

### Task 6 — Layout and Advanced localization

- Baseline localization spec: `20 passed`.
- RED: focused `Layout|Advanced` localization reported `2 passed / 2 failed`
  because the required English Layout and Advanced copy was absent.
- GREEN: focused localization `4 passed`; full localization `22 passed`;
  Layout/Advanced regressions `58 passed / 3 desktop-project touch skips`.
- Fix RED: shared fallback CRUD reported `2 failed` for untranslated generated
  add/edit copy. Strengthened full-cache assertions already passed and were
  correctly recorded as invariant strengthening, not a manufactured RED.
- Fix GREEN: shared fallback CRUD `2 passed`; final localization `24 passed`;
  functional regressions `58 passed / 3 skips`; full gate passed with security
  `25`, Vitest `112`, and Playwright `350 passed / 35 policy skips`.

### Task 7 — documentation and cleanup

- TDD was not required for human documentation and obsolete source/CSS cleanup.
- Before cleanup, the required search found the unused `PlaygroundShell.tsx`,
  standalone selectors, and their obsolete absence assertions. After cleanup,
  the same search produced zero matches (expected `rg` exit `1`).
- No README/source-string assertion test was added. Existing behavior E2E is
  the route, locale, state, and rendered-UI evidence.
- The final Chrome review exposed a separate `360px` widget-header containment
  regression. The pre-fix English Widget screenshot is
  `/private/tmp/comins-grid-task7-visual-20260812/mobile/en-examples-widget.png`.
  At this viewport, every Grid card was about `106.66px` wide, the inner widget
  was `90.66px`, and the header was `88.66px`; the fixed, non-wrapping four
  action row was `116px` wide and ended at `x=165` while the first header ended
  at `x=117.664`. The title already had `min-width: 0`, and the same layout was
  contained at `600px` (`168.66px` header), so the root cause was the narrow
  three-card geometry combined with the action row's fixed nowrap footprint,
  not Korean/English title length or a library GridStack column defect.
- RED:
  `COMINS_GRID_LAYOUT_PORT=6242 npm run test:e2e -- test/playwright/specs/visual-typography.spec.ts --project=mobile-chrome --workers=1 --grep "keeps every Widget header action"`
  failed `1/1`: expected action right `<= 118.65625`, received `165`.
- The minimal fix is example-only CSS below `480px`: the widget header stacks,
  the action container wraps, and all four accessible action buttons remain
  visible. No library source, public CSS, API, route, or dependency changed.
- GREEN: the strengthened focused mobile test passed `1/1` on port `6249`; the
  affected desktop Chromium visual regression passed `2/2` on port `6250`.
  The post-fix required Chromium gate
  passed `166 / 195` with `29` policy skips, and the final Firefox/WebKit gate
  passed `188 / 194` with `6` policy skips.

## Fresh Task 7 verification

### Package baseline

- `git diff --exit-code origin/main -- src/ && npm run build`: PASS.
- `npm run verify`: PASS.
  - security: `25/25`
  - license check: PASS
  - TypeScript: PASS
  - Vitest: `112/112` across `16/16` files
  - build: PASS

### Required Chromium projects

Command:

```sh
COMINS_GRID_LAYOUT_PORT=6251 npm run test:e2e -- \
  --project=chromium --project=mobile-chrome --project=chromium-resource
```

Result: PASS, `166 passed / 29 intentional skips` across `195` collected tests.
The 100-widget resource scenario also passed after repeated column changes and
interactions.

### Local Firefox and WebKit projects

Command:

```sh
COMINS_GRID_LAYOUT_PORT=6252 npm run test:e2e -- \
  --project=firefox --project=webkit
```

Result: PASS, `188 passed / 6 intentional mobile-touch skips` across `194`
collected tests. WebKit is browser-engine compatibility evidence only; it is
not a branded Safari certification.

The first post-fix combined run on port `6246` had one isolated WebKit timeout
in `orders drag lifecycle callbacks after the committed layout` (`187 passed /
6 skipped / 1 failed`). The new `360px` geometry test passed in both engines,
and the mobile CSS media query was inactive in that desktop test. An unchanged
focused WebKit rerun on port `6247` passed `1/1`; the complete unchanged rerun
above then passed. This is classified as transient engine timing evidence, not
a product or test-contract failure.

## Chrome visual review

- Server: `COMINS_GRID_LAYOUT_PORT=6003 npm run dev` from the assigned worktree.
- Browser: actual Chrome launched by Playwright CLI; desktop viewport
  `1440×900`, initial mobile viewport `360×732`, and post-fix containment
  viewport `360×800`.
- The initial discovery matrix captured and reviewed Korean and English
  screenshots for all 20 locale/viewport/route combinations:
  `/docs/getting-started`, `/examples/widget`, `/examples/layout`,
  `/examples/advanced`, and `/api`. Its six mobile example-route screenshots
  are retained as pre-fix discovery artifacts and are not presented as final
  post-fix evidence.
- After the mobile CSS fix, actual Chrome was relaunched against committed
  `583cc1a` on isolated port `6004`. The final `360×800` mobile example matrix
  was recaptured for Widget, Layout, and Advanced in both locales.
- Screenshots were reviewed with the local image viewer from the task-scoped
  `/private/tmp/comins-grid-task7-visual-20260812/` output; they are not staged.

### Confirmed observations

- All 20 combinations had `document.documentElement.lang` equal to the selected
  locale and root `scrollWidth === clientWidth`; no page-level horizontal
  overflow was measured.
- Desktop topnav was `1440×76`; brand, search, and locale controls stayed within
  it, the docs body started immediately after it, and the `260px` sidebar and
  content did not overlap.
- Mobile topnav was `360×186`; brand, search, and locale controls stayed within
  it. The `360px` sidebar stacked before the `360px` content with no overlap.
- Desktop controls and Grid regions remained within the content/live-example
  width. Mobile example controls wrapped to full-width rows and remained inside
  the `360px` viewport; each Grid was `320px` inside a `320px` live region.
- Mobile code blocks used `overflow-x: auto`. The Widget sample measured
  `318px` client width and `452px` scroll width, and a real scroll reached
  `scrollLeft=134` of `maxScrollLeft=134`.
- The Widget add dialog was opened and inspected in English desktop and Korean
  mobile. It stayed centered and fully inside both viewports; all fields,
  locale controls, close, cancel, and save controls were visible.
- The Advanced external drop target was visible and unobstructed at `300×300`
  on desktop (`x=292`) and mobile (`x=20`).
- After clearing the initial browser console and installing explicit listeners,
  all 20 route/locale navigations plus dialog, target, and code-scroll checks
  recorded `0` JavaScript console errors and `0` page errors.
- Post-fix Chrome geometry at `360px` confirmed locale storage and `<html
  lang>` matched each ko/en capture, root overflow was `0`, and every action
  container plus each of its four buttons stayed inside its header and card:
  Widget `3/3` cards per locale, Layout `4/4` per locale, and Advanced `4/4`
  per locale. In the representative Widget cards, the header was
  `88.66px` wide, the wrapped action area was `76.66×56px`, and each of the four
  buttons was `26×26px`, arranged in two rows fully inside the card.
- All six final post-fix mobile screenshots were individually inspected:

  - Widget:
    `/private/tmp/comins-grid-task7-visual-20260812/mobile/ko-examples-widget-post-fix.png`
    and
    `/private/tmp/comins-grid-task7-visual-20260812/mobile/en-examples-widget-post-fix.png`.
  - Layout:
    `/private/tmp/comins-grid-task7-visual-20260812/mobile/ko-examples-layout-post-fix.png`
    and
    `/private/tmp/comins-grid-task7-visual-20260812/mobile/en-examples-layout-post-fix.png`.
  - Advanced:
    `/private/tmp/comins-grid-task7-visual-20260812/mobile/ko-examples-advanced-post-fix.png`
    and
    `/private/tmp/comins-grid-task7-visual-20260812/mobile/en-examples-advanced-post-fix.png`.
- Layout measurements were `4/4` contained cards for ko and `4/4` for en.
  Advanced measurements were `4/4` for ko and `4/4` for en. The Advanced
  external-drop target remained visible, inside the root, and exactly
  `300×300px` at `x=20` in both locales. Header titles, all actions, sidebar,
  search, and locale controls were visually free of clipping or overlap;
  desktop layout remains covered by the focused Chromium regression.

### Visual limitations and concerns

- A fresh Chrome session records one initial network console error for the
  missing `/favicon.ico` (`404`). Subsequent monitored application navigations
  recorded zero console/page errors. No unrelated favicon asset was added in
  this task.
- The observed `360px` action clipping is resolved by the example-only mobile
  layout rule and protected by the focused geometry test. No remaining visual
  overflow or overlap was observed in the approved Chrome matrix.

## Final-review fix wave — restored data and dialog access

- Evidence window: `2026-08-12 23:08:18–23:20:01 KST`.
- Added an example-only restore boundary that requires rendered
  `description` and `value` data to be strings before reducer dispatch in both
  Layout and Advanced. Invalid input leaves the raw editor value, geometry,
  and column-cache state intact and does not reach React rendering.
- Preserved the existing fail-safe fallback for unknown and prototype-like
  `fixtureCopyKey` / `generatedDescriptionKey` values.
- Added dependency-free Dialog initial focus, forward/reverse Tab containment,
  Escape close, explicit opener focus restoration across browser engines, and
  a non-tabbable backdrop while preserving backdrop click and in-dialog locale
  switching.
- Reworked the locale persistence contract to begin at default Korean, select
  English through the UI, assert the exact `en` storage value, reload without
  init-script reinjection, and assert English language, selected control, and
  copy.
- Strengthened locale search evidence to assert the Korean query and popup are
  removed on switch, the Korean query produces no English result, and an
  English query returns the English result.
- Replaced the fixed `750ms` / Playwright error-message backdrop assertion with
  deterministic `elementFromPoint` and real coordinate-click assertions.

### Final-review RED/GREEN

- Restore boundary RED:
  `npm run test:run -- test/vitest/playground-state-snapshot.test.ts` reported
  `3 failed / 15 passed` because the example sanitizer did not exist.
  Chromium Advanced RED reported `2 failed`; both malformed data objects were
  accepted, and React emitted an invalid-child exception.
- Restore boundary GREEN: focused Vitest `18/18`; Chromium invalid-data E2E
  `2/2`; full Advanced regression `12/12`; full Layout regression `7/7`.
- Dialog RED: Chromium initial focus, Tab containment, and Escape/focus restore
  reported `3 failed`. GREEN after the first implementation was `4/4`,
  including coordinate backdrop closure.
- Test-quality mutation RED: removing locale storage write failed with exact
  storage `null`; retaining both locale-change and outside-click search paths
  failed with the Korean query still present; disabling backdrop hit testing
  failed `elementFromPoint`. After restoring production behavior, the combined
  persistence/search/backdrop focused run passed `4/4`.
- Chromium localization regression passed `28/28`.
- The one requested `npm run verify:full` execution started at `23:14 KST`.
  Its package phase passed security `25/25`, license, TypeScript, Vitest
  `115/115`, and build. E2E ended `376 passed / 35 policy skips / 2 failed`:
  WebKit closed both dialogs but did not focus the opener because WebKit does
  not guarantee that a mouse-clicked button becomes `document.activeElement`.
- The minimal follow-up passes explicit add/edit trigger refs to Dialog.
  The two failed WebKit cases then passed `2/2`; the complete localization spec
  passed `112/112` across Chromium, mobile Chrome, Firefox, and WebKit.
- Fresh final `npm run verify` passed security `25/25`, license, TypeScript,
  Vitest `115/115`, and build. `verify:full` was not invoked a second time, in
  accordance with the exact-once instruction; its non-zero result and the
  targeted cross-engine closure are retained together as the final evidence.
- The final combined restored-data and Dialog contract rerun passed `12/12`
  across Chromium and WebKit.
- `git diff 239003ba217d697c3805e5ec71555e1cc3bfe139 -- src package.json package-lock.json`
  remains empty.

### Final-review residuals

- The one full-gate command itself did not exit zero before the WebKit-specific
  opener-ref correction. The complete affected cross-engine spec and fresh
  package baseline pass on final code, but the exact full command was not
  repeated because this wave explicitly required one execution.
- Real Safari remains unverified; Playwright WebKit is engine evidence only.
- No remote write, package manifest/lock change, public `src/` change, publish,
  or release action was performed.

## Dependency advisory

- Fresh `npm audit --json` reported one high-severity transitive advisory:
  `nanoid@3.3.16`, GHSA-2v37-7h3g-55p8, with a fix available.
- `npm explain nanoid` shows it is dev-only through
  `vite@8.1.4 -> postcss@8.5.24 -> nanoid@3.3.16`; it is not a direct runtime
  dependency or exported package surface.
- No dependency or lockfile change was authorized or made in this task.

## Remaining boundary

- Real Safari on macOS/iOS was not run. Playwright WebKit does not substitute
  for a branded Safari verification.
- No remote write or release action was performed. Push/PR/merge/publish remain
  separate approval-gated actions.
