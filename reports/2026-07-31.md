# 2026-07-31

## 12:42 KST - External drop target design

### Summary

- Defined the additive `0.2.0` external drop target contract for ordinary HTML
  elements.
- Kept React widget state authoritative and selected a typed, non-destructive
  callback instead of exposing GridStack's native DOM-removal option.
- Fixed the approved hit rule to the final mouse, pointer, or touch release
  point and included dynamic target resolution, callback ordering, responsive
  layout deletion consistency, and multi-browser verification boundaries.
- Performed no runtime implementation or public release operation.

### Changed Files

- `docs/superpowers/specs/2026-07-31-external-drop-target-design.md`
- `reports/2026-07-31.md`

### Validation

- License impact: `N/A`; the design adds no dependency, copied or modified
  third-party code, generated asset, bundle change, or package-content change.
- Created the isolated `codex/0.2.0-external-drop-target` worktree from
  `1ff7b50`.
- `npm ci`: passed.
- Baseline `npm run verify`: passed.
  - Security tests: 23/23 passed.
  - License gate: passed.
  - TypeScript typecheck: passed.
  - Vitest: 49/49 passed.
  - Vite and TypeScript build: passed.
- Design placeholder, contradiction, ambiguity, and scope review: passed after
  clarifying rendered-target eligibility and imperative touch-point capture.
- Focused sensitive-data guidance test: 6/6 passed.
- `git diff --check`: passed after the final clarification edits.

### Residual Decisions

- The written design requires user review before an implementation plan is
  created.
- Responsive per-column persistence and multi-browser support remain separate
  `0.2.0` design and plan streams.
- Firefox/WebKit project setup and browser binary installation were not
  performed during this design-only task.

## 13:04 KST - External drop target implementation plan

### Summary

- Converted the approved external drop target design into a five-task TDD
  implementation plan.
- Separated public types and structural validation, DOM hit resolution,
  adapter and consumer integration, documentation, and complete verification
  into independently reviewable commits.
- Kept responsive per-column persistence and Firefox/WebKit support outside
  this plan as separate `0.2.0` work streams.

### Changed Files

- `docs/superpowers/plans/2026-07-31-external-drop-target.md`
- `reports/2026-07-31.md`

### Validation

- License impact: `N/A`; this plan adds no dependency, copied or modified
  third-party code, generated asset, bundle change, or package-content change.
- Checked the plan against every approved design section.
- Checked task interfaces and public type names across all five tasks.
- Corrected the adapter fallback event type, exact-once and locked/resize test
  coverage, product diff baseline, commit-history expectation, and consumer
  smoke expectation during self-review.
- Confirmed all referenced npm scripts and Playwright project names exist.
- Code-fence balance and placeholder scan: passed.
- Focused sensitive-data guidance test: 6/6 passed.
- `git diff --check`: passed.

### Residual Decisions

- Implementation has not started.
- Execution requires the user to select the agent-driven or inline workflow.
- Push, PR, tag, Release, publish, and provider operations remain outside the
  authorized scope.

## 15:56 KST - External drop target implementation and verification

### Summary

- Added `externalDropTargets` and `onWidgetExternalDrop` as additive
  `DashboardGrid` props, backed by the new public
  `DashboardExternalDropTarget` and `DashboardWidgetExternalDropEvent` types.
- Added structural configuration validation, release-point normalization, and
  release-time resolution for ordinary same-document light-DOM targets.
- Integrated desktop mouse/pointer and mobile Chrome touch drops without
  enabling GridStack removal or mutating controlled React state in the package.
- Kept deletion consumer-owned: the README demo handles the callback with the
  existing `removeWidget(widgetId)` command.

### Changed Files

- Public/runtime:
  - `src/core/types.ts`
  - `src/core/configuration.ts`
  - `src/components/DashboardGrid.tsx`
  - `src/gridstack/external-drop-target.ts`
  - `src/gridstack/adapter.ts`
- Example:
  - `example/src/readme-demo.tsx`
  - `example/src/styles.css`
- Tests:
  - `test/vitest/configuration.test.ts`
  - `test/vitest/dashboard-grid-configuration.test.tsx`
  - `test/vitest/dashboard-grid-handle.test.tsx`
  - `test/vitest/external-drop-target.test.ts`
  - `test/playwright/specs/dashboard-grid.spec.ts`
- Product documentation:
  - `README.md`
  - `docs/03-component-api-draft.md`
  - `docs/05-open-questions.md`
- Design, plan, and evidence:
  - `docs/superpowers/specs/2026-07-31-external-drop-target-design.md`
  - `docs/superpowers/plans/2026-07-31-external-drop-target.md`
  - `reports/2026-07-31.md`

### Focused TDD Evidence

- Contract RED:
  `npx vitest run test/vitest/configuration.test.ts test/vitest/dashboard-grid-configuration.test.tsx`
  failed with 4 expected missing-validation failures. The GREEN rerun passed
  2 files and 19 tests.
- Resolver RED:
  `npx vitest run test/vitest/external-drop-target.test.ts` failed because the
  production module did not exist and executed 0 tests. The GREEN rerun passed
  1 file and 13 tests.
- Integration RED: `npm run lint` failed on the missing public callback and
  demo bridge types. The browser RED command
  `npx playwright test --project=chromium --project=mobile-chrome --grep "external drop|plain div"`
  reported 6 failed, 2 passed, and 8 project-mismatch skips before integration.
- Integration GREEN: the four focused Vitest files passed 34/34 tests;
  `npm run lint` passed; the focused desktop/mobile browser command passed 8
  tests with 8 project-mismatch skips.
- Documentation followed the approved no-new-source-string-test ruling. The
  unchanged README test passed 3/3 and `npm run lint` passed.

### Full Verification

- License impact: `N/A`; no dependency, lockfile, copied or modified
  third-party source, generated asset, or package boundary was added.
- Pre-final-review `npm run verify`: passed.
  - Security: 23/23 passed.
  - Third-party notice gate: passed.
  - TypeScript typecheck: passed.
  - Vitest baseline before final-review coverage: 12/12 files and 68 tests
    passed.
  - Vite and declaration build: passed.
- `npx playwright install chromium`: passed; no repository dependency or
  tracked file changed.
- The initial permitted-environment `npm run verify:full` passed, but a fresh
  controller rerun exposed a nondeterministic compact-test initialization race:
  69 passed, 28 skipped, 1 failed, and 1 did not run. The compact snapshot was
  unavailable because the imperative handle existed before its asynchronous
  GridStack adapter was live.
- Added the minimal test-only synchronization in
  `test/playwright/specs/dashboard-grid.spec.ts`: poll
  `getHandle()?.getGridStack()` until non-null before invoking `compact`.
  Product code was not changed.
- Focused GREEN:
  `npx playwright test --project=chromium --grep "compacts only through the explicit handle command and commits once"`
  passed 1/1.
- Pre-final-review permitted-environment `npm run verify:full`: passed.
  - The package baseline passed 23/23 security tests, 12/12 Vitest files and
    68 tests, the license/type/build gates.
  - Playwright: 71 passed, 28 skipped, 0 failed across Chromium, mobile Chrome,
    and the Chromium resource project.
  - The 100-widget resource test passed with stable DOM nodes, event listeners,
    documents, heap peak, and steady-state counters through repeated column and
    interaction loops.
  - The first restricted-environment attempt stopped only because local server
    bind to `127.0.0.1:6001` was denied with `listen EPERM`; the identical
    command was rerun without weakening the gate.
- `npm run test:consumer`: passed in the permitted environment after installing
  8 packages into the script's fresh isolated React 18 consumer; package entry
  and stylesheet export imports passed.
  - The restricted-network attempt was stopped after its fresh-cache install
    made no progress; the identical command was rerun without reusing a stale
    consumer.

### Behavior Evidence

- The full Chromium run passed the assertion that, when a layout commit occurs,
  callback order is `onWidgetLayoutChange`, `onLayoutCommit`,
  `onWidgetExternalDrop`, then `onWidgetDragStop`.
- The desktop deletion flow emitted exactly one
  `external-drop:trash:overview` event, hid the controlled React widget, and
  removed `overview` from the GridStack engine only after the consumer invoked
  `removeWidget`.
- Desktop outside-target, remounted-target, non-movable, locked, and resize
  cases passed. The mobile Chrome touch drop onto a descendant of a plain
  consumer-owned `div` passed.
- Browser console and page-error diagnostics remained green under the existing
  Playwright fixtures.

### Public And Dependency Review

- `git diff 1ff7b50..HEAD -- src package.json package-lock.json README.md
  docs/03-component-api-draft.md docs/05-open-questions.md` confirmed the public
  change is limited to two additive props and two additive package-owned types.
- The external-drop event contains only widget ID, target ID, active columns,
  and package-owned layout; no raw `GridStack` type leaks through it.
- `package.json` and `package-lock.json` are byte-identical to `1ff7b50`;
  `npm ls --depth=0` passed, and the package version remains `0.1.5`.
- No direct GridStack `removable` option, per-column persistence, Firefox or
  WebKit project, DOM `CustomEvent`, dependency, release metadata, or
  package-version change was included.

### Residual Scope And Operations

- P1 responsive per-column persistence remains a separate approved `0.2.0`
  work stream.
- Firefox/WebKit configuration and verification remain a separate approved
  multi-browser work stream.
- No push, pull request, tag, Release, staging, publish, provider write, or
  destructive action was performed.

## 16:19 KST - External drop final-review closure

### Summary

- Closed runtime evidence for the complete external-drop callback payload
  without changing package production source.
- Extended the README demo consumer bridge with independently cloned snapshots
  of the most recent committed layout and external-drop event. Snapshot getters
  return fresh clones, and the existing interaction reset clears both.
- Kept the generic public-prop Vitest as an honest compile-time contract by
  removing callback-body assertions that were never executed.
- Documented the public drag callback order and added characterization coverage
  for the resolver's existing selector-error conversion.

### Changed Files

- Example and runtime evidence:
  - `example/src/readme-demo.tsx`
  - `test/playwright/specs/dashboard-grid.spec.ts`
  - `test/vitest/dashboard-grid-handle.test.tsx`
  - `test/vitest/external-drop-target.test.ts`
- Documentation and evidence:
  - `docs/03-component-api-draft.md`
  - `reports/2026-07-31.md`

### RED, GREEN, And Characterization Evidence

- Payload RED:
  - `npm run lint` failed with the expected `TS2339` errors because
    `getLastCommittedLayout` and `getLastExternalDropEvent` did not yet exist on
    `ReadmeDemoBridge`; the dependent layout callback also lost its type.
  - The primary desktop Chromium test failed 0/1 with
    `getLastCommittedLayout is not a function`.
- Payload GREEN:
  - `npm run lint` passed.
  - `test/vitest/dashboard-grid-handle.test.tsx` passed 2/2.
  - The primary desktop external-drop Chromium test passed 1/1.
- Resolver catch coverage is characterization, not a TDD RED:
  production already converted `querySelectorAll` failures into
  `DashboardGridConfigurationError`.
- Final affected Vitest command passed 2/2 files and 16/16 tests.
- Focused desktop Chromium external-drop command passed 7/7 tests.

### Runtime Payload Evidence

- The primary desktop deletion flow still asserts exactly one external-drop
  event, controlled React removal, and GridStack engine removal.
- It additionally asserts exact `widgetId="overview"` and `targetId="trash"`.
- Event `columns` equals the live GridStack column count and the independently
  stored committed snapshot column count.
- The complete event `layout` equals the matching `overview` layout in the
  independently stored committed snapshot.

### Full Verification

- The first restricted-environment `npm run verify:full` completed security,
  license, typecheck, 12/12 Vitest files with 69/69 tests, and build, then
  stopped only at local server startup with
  `listen EPERM 127.0.0.1:6001`.
- The identical command passed in the permitted environment:
  - Security: 23/23 passed.
  - License, TypeScript typecheck, Vite build, and declaration build passed.
  - Vitest: 12/12 files and 69/69 tests passed.
  - Playwright: 71 passed, 28 skipped, 0 failed across Chromium, mobile Chrome,
    and the Chromium resource project.
  - The 100-widget resource stability gate passed through repeated column and
    interaction cycles.
- Focused sensitive-data guidance verification passed 6/6.
- `git diff --check` passed.
- Consumer smoke was not rerun because this review wave changed no packed
  `src/` source, dependency, lockfile, package version, export, or artifact
  boundary. The prior fresh React 18 consumer result remains historical
  evidence only.

### Public, Dependency, And Operation Boundaries

- No production file under `src/`, package manifest, lockfile, dependency,
  Playwright project configuration, P1 persistence behavior, Firefox/WebKit
  scope, or release state changed.
- No push, pull request, tag, Release, staging, publish, provider write, or
  destructive action was performed.

## 16:40 KST - External drop Playground integration

### Summary

- Added the external widget drop feature to the default
  `/examples/complete` Playground.
- Rendered a consumer-owned ordinary `div` deletion target with a desktop size
  of exactly `300 x 300` CSS pixels.
- Connected `onWidgetExternalDrop` to the existing
  `dashboard.commands.removeWidget` command and added an accessible live
  result status.
- Kept the package callback non-destructive and changed no production source
  under `src/`.

### Changed Files

- Playground consumer and example-only presentation:
  - `example/src/main.tsx`
  - `example/src/styles.css`
- Browser regression contract:
  - `test/playwright/specs/docs-playground-routing.spec.ts`
- Approved design and implementation sequence:
  - `docs/superpowers/specs/2026-07-31-external-drop-playground-design.md`
  - `docs/superpowers/plans/2026-07-31-external-drop-playground.md`

### TDD And Focused Verification

- RED:
  the focused Chromium Playground scenario failed because the accessible
  external target did not exist.
- GREEN:
  the same scenario passed after the consumer integration and confirmed the
  target is a `DIV`, is `300 x 300`, removes `sales`, changes the count from
  four to three, updates the live status, and emits no browser diagnostics.
- The full docs Playground routing spec passed 22 tests with two intentional
  project-scope skips.
- `npm run lint` and `npm run build` passed.

### Regression Diagnosis And Correction

- The first permitted full-browser run exposed two real existing-behavior
  regressions after initially placing the deletion target beside the grid:
  - the reduced grid width hid the 12-column double-click title handle;
  - the existing right-side external-drag test released over the new deletion
    target and removed `sales`.
- Moved the target into an independent row above the grid. This preserves the
  original grid width and keeps the right-side outside-grid region unmatched.
- The new Playground scenario and both affected legacy tests then passed
  together, 3/3.
- A fresh 1440 x 1200 full-page Chromium rendering showed the target, status,
  full-width grid, and widget controls without clipping or overlap.

### Full Verification

- The restricted-environment `npm run verify:full` completed the package
  baseline and stopped only when the Playwright web server was denied local
  bind permission with `listen EPERM 127.0.0.1:6001`.
- The final permitted-environment `npm run verify:full` passed:
  - security: 23/23;
  - license, TypeScript typecheck, Vite build, and declaration build;
  - Vitest: 12/12 files and 69/69 tests;
  - Playwright: 72 passed, 29 intentional skips, 0 failed across Chromium,
    mobile Chrome, and the Chromium resource project;
  - the 100-widget resource-stability gate passed with steady DOM, listener,
    document, heap, and interaction counters.
- `git diff --check` passed before the final gate.

### Scope And Remaining Operations

- `package.json`, `package-lock.json`, the package version, dependencies,
  runtime source, P1 persistence, and Firefox/WebKit project configuration are
  unchanged by this Playground integration.
- Push, PR creation, required-check review, and `main` merge remain the next
  authorized operations.
- Tag, Release, npm staging, and public publication remain outside scope.
