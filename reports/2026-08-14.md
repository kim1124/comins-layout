# 2026-08-14 Playground example expansion

## Summary

- Documented the consumer-owned `renderWidgetActions` contract, `showControls={false}` behavior, the live read-only `DashboardGridHandle.grid` property, and the compatible `getGridStack()` method.
- Documented safe `compact()` / `commitLayout()` usage and the controlled-state divergence risk of raw GridStack `addWidget`, `removeWidget`, and `destroy` calls.
- Migrated stale pre-redesign browser assertions to the indexed Widget actions and canonical Layout / Advanced routes without changing product code.
- Completed the requested Chrome visual review for 9 routes, 2 locales, and 2 viewport sizes (36 observations).
- Ran the final full gate exactly once. It exposed stale browser contracts rather than a product regression; closure used package verification and only the failed Playwright files/projects. The full gate was not rerun.

## Changed files

- `README.md`
- `docs/03-component-api-draft.md`
- `example/src/docs/content.tsx`
- `test/vitest/readme.test.ts`
- `test/playwright/specs/docs-playground-routing.spec.ts`
- `test/playwright/specs/playground-localization.spec.ts`
- `test/playwright/specs/playground.spec.ts`
- `test/playwright/specs/dashboard-grid.spec.ts`
- `reports/2026-08-14.md`
- `test/playwright/specs/example.spec.ts` — approved failed-spec migration from sales/maximize fixtures to indexed custom actions.
- `test/playwright/specs/visual-typography.spec.ts` — approved failed-spec migration from four default actions to three custom actions.
- `test/playwright/specs/playground-advanced-examples.spec.ts` — approved Firefox failed-spec closure for CSS subpixel geometry and an in-viewport native pointer path.

The last three test files are the justified expansion from the original nine-file Task 13 scope. No package source, dependency, lockfile, or configuration file changed in Task 13.

## TDD RED evidence

- Command: `npm run test:run -- test/vitest/readme.test.ts`
- Result: 5 tests executed, 4 passed and 1 failed.
- Expected RED: the new README contract test could not find `renderWidgetActions` before the public documentation was updated.

## Focused GREEN evidence

- Focused Vitest command from Task 13: 5 files passed, 72 tests passed.
- Focused Chromium + mobile-Chrome command from Task 13: 162 tests executed; initial result was 158 passed, 1 intentional project-policy skip, and 3 stale documentation-copy failures.
- Exact routing closure: 28 tests executed; 27 passed and 1 intentional skip.
- Added localized API contract check: Chromium and mobile Chrome, 2 passed.
- Final package closure command `npm run verify`: security 25/25, license check passed, typecheck passed, Vitest 18 files and 163/163 tests passed, build passed.

### Failed-spec browser closure

- Chromium failed files (`dashboard-grid`, `example`, `visual-typography`): 40 executed, initially 32 passed / 3 policy skips / 5 stale-contract failures. The exact five rerun produced 4 passed / 1 remaining stale Handle status assertion; the final exact single rerun passed.
- Firefox + WebKit + mobile Chrome failed files: 120 executed, 89 passed / 31 policy skips / 0 failed.
- Firefox external-drop closure: the browser reports a CSS 300px target as 299.999938px. The test retains visibility and containment assertions and permits at most 1px of cross-engine subpixel delta. Both affected specs passed in targeted closure.
- Firefox Float closure: the original 1100px viewport put the native pointer endpoint outside the viewport. Gecko auto-scrolled the document and placed the placeholder at `gs-y=0`. A 1400px diagnostic attempt still allowed 133px of document scroll and failed; the final 1800px viewport kept the native pointer path fully in view and the exact test passed. No GridStack state was synthesized.
- Chromium resource project: the full run did not execute it because its dependency project failed first. The exact test was run with `--no-deps`; 1/1 passed. It retained stable document/node/listener counters and stayed within the configured heap budgets.

## Final full gate

- Command run exactly once: `COMINS_GRID_LAYOUT_PORT=6031 npm run verify:full`.
- Package phase passed: security 25/25, license check, typecheck, Vitest 18 files and 163/163 tests, and build.
- Initial Playwright result: 557 total — 447 passed, 35 intentional skips, 74 failed, and 1 did not run.
- Classification: stale test contract. Legacy browser specs still expected sales/traffic/maximize/minimize/four-action UI and controls that had moved to canonical child routes. The Firefox-only geometry failures were test-environment precision/input-path differences described above.
- Closure: `npm run verify` and the exact affected Playwright files/projects passed as recorded above. Per the Task 13 rule, no second full gate was launched.

## Desktop and mobile visual review

Actual headed Chrome was inspected with browser DOM geometry and selected screenshots at 1280x900 and 360x800, in Korean and English, for:

- `/examples/widget`
- `/examples/layout`
- `/examples/layout/columns`
- `/examples/layout/lock`
- `/examples/advanced`
- `/examples/advanced/handle`
- `/examples/advanced/state`
- `/examples/advanced/events`
- `/examples/advanced/external-drop`

Result: 36/36 route-locale-viewport observations passed. Every observation had exactly one Grid, no root horizontal overflow, the locale toggle immediately left of search, and contained toolbar/control geometry. The minimum effective pastel text contrast measured 5.965:1. All four state-route observations had two detail panels and both were collapsed. All four external-drop observations had a visible, contained target. Selected desktop/mobile screenshots were visually inspected; temporary screenshots and Playwright CLI state were not retained in the commit.

## Residual risks

- Playwright WebKit is regression coverage for the WebKit engine; it is not certification in real Safari. Real Safari was not run.
- The 360x800 review used real Chrome with a mobile viewport, not a physical mobile device.
- The SDD ledger still records deferred minor coverage gaps outside Task 13: explicit preservation assertions for all edit metadata, same-ID/route-unmount/English refresh timer cases, pairwise non-overlap at reduced columns, Handle example literal/bridge and compact-commit cleanup, bounded-event invalid-input edge cases, and external-drop same-row/event-normalization stress cases. These were not expanded into product changes.
- A benign missing favicon request was the only Chrome visual-review console error; it did not affect the application checks.
- No push, pull request, merge, tag, GitHub Release, npm publish, or other deployment action was performed.

## Final review targeted closure

- Removed the stale dynamic-column and layout-lock metadata cases from `/examples/layout`; the root now renders and indexes only its save/restore example. The canonical `/examples/layout/columns` and `/examples/layout/lock` pages remain independent and searchable in Korean and English.
- Scoped responsive toolbar overrides to `[data-example-mode="widget"]`: all three semantic groups remain on one row at 768px, while 360px may wrap between groups without splitting a group's buttons.
- TDD RED: 5/5 targeted Chromium tests failed against the reviewed implementation (three root cases, duplicate root search targets, 82px iPad row separation, and 39px internal mobile group separation).
- Focused GREEN: targeted Chromium 5/5; related Chromium and mobile-Chrome 43 passed / 1 intentional desktop-only skip; exact Firefox and WebKit coverage 10/10.
- Installed Google Chrome DOM observation: at 768x1024 all three groups shared `y=1227.765625`; at 360x800 the groups wrapped as units and every button within a group shared its row. Document, Widget root, and toolbar horizontal overflow were all zero at both sizes. No screenshots were retained.
- Fresh `npm run verify`: security 25/25, license check, typecheck, Vitest 18 files and 163/163 tests, and build all passed. Per final-review scope, `verify:full` was not rerun.
