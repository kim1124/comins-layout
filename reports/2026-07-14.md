# 2026-07-14 Work Report

## KST - standalone repository documentation and remote alignment

- 작업 일시: 2026-07-14 KST
- 작업 요약: KMSF 하위 패키지에서 분리된 `comins-grid-layout`의 독립 저장소 운영 기준을 문서화하고, 원격 저장소 메타데이터를 `kim1124/comins-layout` 기준으로 정합화했다.
- 변경 파일:
  - `README.md`
  - `GUIDE.md`
  - `docs/README.md`
  - `docs/01-requirements.md`
  - `docs/04-verification-strategy.md`
  - `package.json`
- 반영 내용:
  - 이 저장소를 기능 구현, 문서, 검증, 릴리스의 단일 기준으로 명시했다.
  - KMSF `packages/gridstack`를 이력 참고 대상으로 한정하고, 기본 소스 동기화 대상에서 제외했다.
  - GitHub 저장소 `kim1124/comins-layout`, 기본 통합 브랜치 `main`, npm 패키지명 `comins-grid-layout`의 관계를 문서화했다.
  - `package.json`의 repository, bugs, homepage URL을 `kim1124/comins-layout` 기준으로 변경했다.
  - 문서 작업 보고 경로를 독립 저장소의 `reports/YYYY-MM-DD.md`로 정정했다.
- 수행한 검증:
  - 대상 GitHub 원격의 `main` 이력 확인: pass, 기존 이력 없음.
  - `npm run verify:full`: pass, TypeScript lint + Vitest 5개 파일/15개 테스트 + production build + Playwright 49 passed/9 skipped.
  - 초기 격리 환경의 Playwright 개발 서버 바인드는 `127.0.0.1:6001` `EPERM`으로 실패했으며, 권한 허용 환경에서 동일 명령을 재실행해 통과했다.
  - `git diff --cached --check`: pass.
- 결과:
  - 독립 저장소 전환 이후의 문서 및 패키지 원격 메타데이터 기준을 정리했다.
- 잔여 이슈 및 후속 작업:
  - npm publish는 이번 범위에 포함하지 않는다.
  - KMSF 원본 저장소의 패키지 제거 또는 참조 전환은 이번 범위에 포함하지 않는다.

## KST - residual risk and Codex model configuration research

- 작업 일시: 2026-07-14 KST
- 작업 요약: `comins-grid-layout`의 기능·검증·배포 잔여 리스크와 GPT-5.6 Codex 작업 설정을 조사했다. 소스 코드와 Codex 설정은 변경하지 않았다.
- 확인한 소스:
  - 저장소 작업 보고, 요구사항·API·검증·미해결 질문 문서, core state·GridStack adapter·component 구현, Vitest·Playwright 사양, package manifest 및 lockfile
  - 전역 Codex `config.toml`, 프로젝트 로컬 `.codex/config.toml` 부재, Codex CLI 버전
  - 최신 공식 Codex 매뉴얼과 OpenAI GPT-5.6 모델 안내
- 확인된 결과:
  - 현재 `main`은 `origin/main`과 일치하며, 마지막 full verification은 Vitest 15건 및 Playwright 49 passed/9 skipped 결과다.
  - `serializeDashboardState()`가 `previousLayouts`를 저장하지 않는다. 최대화된 위젯을 serialize -> rehydrate -> restore하는 실제 실행에서 원래 `{ x: 2, y: 1, w: 2, h: 2 }`가 아닌 최대화 layout `{ x: 0, y: 0, w: 6, h: 3 }`로 남는 것을 재현했다.
  - `100+` 위젯과 반복 column 변경은 프로젝트 baseline 요구사항이지만 관련 performance/soak 회귀 테스트가 없다.
  - API 초안과 open questions는 구현된 public API 및 maximize/minimize/arrange 동작과 일부 불일치한다.
  - mobile browser 프로젝트에서는 desktop-only interaction 9건이 계속 skip된다.
  - `npm --cache /private/tmp/comins-grid-layout-npm-cache pack --dry-run --json`은 통과했으나 tarball에 root `LICENSE` 파일이 포함되지 않는다. `package.json` dependency의 다수가 `latest`로 선언되어 있다.
  - 동일 isolated cache로 실행한 `npm audit --json`은 취약점 0건을 보고했다.
  - 전역 Codex 기본값은 `model = "gpt-5.6-terra"`, `model_reasoning_effort = "xhigh"`, `approval_policy = "on-request"`, `sandbox_mode = "workspace-write"`다. 이 저장소는 trusted project이며 로컬 `.codex/config.toml` override가 없다. Codex CLI는 `0.144.4`다.
- 결과:
  - 즉시 수정 우선순위는 persistence restore contract와 100+ widget/repeated-column 회귀 검증이다.
  - 모델 설정은 유효하지만, Terra의 속도·비용 균형 목적과 기본 `xhigh`의 상시 사용은 조정 후보로 기록한다. 구성 변경은 사용자 승인 전까지 수행하지 않는다.
- 수행한 검증:
  - `node --input-type=module ...`: maximize -> serialize -> rehydrate -> restore persistence failure 재현.
  - `npm --cache /private/tmp/comins-grid-layout-npm-cache pack --dry-run --json`: pass, 27 files.
  - `npm --cache /private/tmp/comins-grid-layout-npm-cache audit --json`: pass, vulnerabilities 0.
  - `codex --version`: `codex-cli 0.144.4`.
- 잔여 이슈 및 후속 작업:
  - source code, dependency version policy, license file, CI, Codex configuration은 이번 조사에서 변경하지 않았다.

## KST - persistence and performance stabilization design

- 작업 일시: 2026-07-14 KST
- 작업 요약: 사용자 결정에 따라 maximize/minimize 복원 정보의 완전 저장, 100+ widget 반복 column 변경 안정성, Chrome DevTools resource counter 합격 기준을 설계 문서로 확정했다.
- 변경 파일:
  - `docs/superpowers/specs/2026-07-14-persistence-performance-stabilization-design.md`
  - `docs/README.md`
- 확정 사항:
  - `DashboardStateSnapshot`은 `previousLayouts`를 저장해 maximize/minimize 이후에도 원래 geometry를 복원한다.
  - Chrome DevTools JS Heap, DOM Nodes, Event Listeners는 동일 session baseline-return sequence를 세 번 반복해 누적 여부를 판정한다.
  - npm publish와 release hardening은 안정화 완료 뒤 별도 범위로 처리한다.
  - Codex는 `gpt-5.6-terra`와 `xhigh`를 유지하고, 메모리 누수 등 중대한 하자 발생 시에만 Sol 전환을 검토한다.
- 수행한 검증:
  - 설계 문서 self-review: pass, placeholder·모순·범위 누락 없음.
  - `git diff --check`: pass.
- 잔여 이슈 및 후속 작업:
  - 사용자 설계 문서 검토·승인 뒤에만 implementation plan과 TDD 구현을 시작한다.

## 2026-07-14 17:09 KST - persistence and 100-widget stabilization implementation

- 작업 요약: maximize/minimize 복원 geometry를 full-state JSON에 저장하고, 100개 widget의 반복 column 변경 및 interaction resource stability 회귀 게이트를 추가했다.
- 변경 파일:
  - `src/core/types.ts`
  - `src/core/layout-state.ts`
  - `src/core/use-dashboard-grid.ts`
  - `playwright.config.ts`
  - `test/vitest/layout-state.test.ts`
  - `test/playwright/specs/dashboard-grid.spec.ts`
  - `test/playwright/specs/docs-playground-routing.spec.ts`
  - `example/src/main.tsx`
  - `README.md`
  - `docs/README.md`
  - `docs/03-component-api-draft.md`
  - `docs/04-verification-strategy.md`
  - `docs/05-open-questions.md`
  - `docs/superpowers/plans/2026-07-14-persistence-performance-stabilization.md`
- 반영 내용:
  - `DashboardStateSnapshot`에 `previousLayouts`를 추가했다. `serializeState()`는 이 map을 deep copy하고, state rehydrate는 현재 widget ID와 일치하는 restore geometry만 정규화해 읽는다. `DashboardStateSnapshotInput`은 legacy TypeScript consumer가 optional restore map을 전달할 수 있게 하며 `restoreLayout()`과 `resetLayout()`의 public input에 적용했다.
  - `previousLayouts`가 없는 이전 JSON snapshot은 빈 map으로 읽는다.
  - `serializeLayout()`은 geometry-only snapshot으로 유지하고, `serializeState()`은 최대화/최소화 후 restore에 필요한 full state를 보존하도록 공개 문서와 Docs Playground를 정정했다.
  - complete example의 JSON restore control을 통해 100개 widget을 로드하고 3회의 `1..12` column cycle 및 drag/resize repeat를 검증하는 Chromium 회귀 테스트를 추가했다. resource 측정은 일반 browser suite 종료 뒤 `chromium-resource` 단일 worker project에서 실행해 동시 Chromium의 Heap 변동을 분리했다.
- 수행한 검증:
  - `npm run test:run -- test/vitest/layout-state.test.ts`: RED 3건 확인 후 GREEN 12 passed.
  - `npm run typecheck`: pass.
  - `npm run test:e2e -- test/playwright/specs/dashboard-grid.spec.ts --project=chromium-resource --no-deps -g "100 widgets"`: pass.
  - `--workers=3 --repeat-each=3` 재현은 3건 중 1건의 일회성 Heap peak을 확인했으나 DOM Nodes/Event Listeners/Documents 누적은 없었다. 이 결과를 근거로 resource project를 단일 worker 및 dependency 순서로 격리했다.
  - `npm run test:e2e -- test/playwright/specs/docs-playground-routing.spec.ts --project=chromium -g "previousLayouts"`: RED 확인 후 GREEN 1 passed.
  - `npm run verify:full`: pass — TypeScript lint, Vitest 5 files/19 tests, production build, Playwright 51 passed/10 skipped.
  - 전체 gate의 Chromium CDP counter (JS Heap byte, DOM Nodes, Event Listeners, Documents):

    | 구간 | JS Heap | DOM Nodes | Event Listeners | Documents |
    | --- | ---: | ---: | ---: | ---: |
    | column baseline (warm-up 이후) | 24,680,116 | 14,080 | 987 | 1 |
    | column cycle 1 | 24,594,240 | 14,080 | 987 | 1 |
    | column cycle 2 | 26,997,264 | 14,080 | 987 | 1 |
    | column cycle 3 | 24,422,468 | 14,080 | 987 | 1 |
    | interaction warmup (drag) | 27,023,720 | 14,091 | 1,000 | 2 |
    | interaction baseline (resize) | 27,256,172 | 14,093 | 1,000 | 2 |
    | repeated interaction 1 | 24,995,920 | 14,093 | 1,000 | 2 |
    | repeated interaction 2 | 27,334,672 | 14,093 | 1,000 | 2 |

  - warm-up 이후 column cycle에서는 DOM Nodes/Event Listeners/Documents가 고정됐다. 첫 GridStack interaction의 one-time allocation 뒤 반복 interaction에서도 세 counter가 고정됐다. post-GC JS Heap은 단조 증가하지 않았고, column 구간은 최대 12%·최종 2%, interaction 구간은 최대·최종 2% 기준을 통과했다.
- 결과:
  - restore geometry persistence와 legacy snapshot 호환성이 state-level TDD로 보장된다.
  - 100-widget repeated-column Chromium regression은 console/page error 없이 통과하며 Chrome DevTools Protocol counter JSON을 test artifact에 남긴다.
- 잔여 이슈 및 후속 작업:
  - Chrome DevTools GUI Performance Monitor의 직접 측정은 Chrome 연결 초기화가 `Cannot redefine property: process`로 실패해 실행하지 못했다. automated Chromium CDP counter는 실행했으며, GUI 측정값은 별도 수동 확인이 필요하다.
  - mobile-chrome의 desktop pointer interaction 9건과 desktop-only docs contract 1건이 skip됐다. 배포/라이선스/dependency version policy는 이번 안정화 범위 밖이다.

## 2026-07-14 17:27 KST - Chrome resource measurement stabilization

- 작업 요약: Chrome DevTools Protocol resource counter가 실제 누수와 비동기 렌더 정리 중인 일회성 heap 변동을 구분하도록 측정 순서를 안정화했다.
- 변경 파일:
  - `test/playwright/specs/dashboard-grid.spec.ts`
  - `docs/04-verification-strategy.md`
- 반영 내용:
  - 각 counter 샘플에서 animation frame/100ms idle 대기 뒤 강제 GC를 수행하고, 250ms idle 대기 및 두 번째 강제 GC 뒤에 수집한다.
  - `JSHeapUsedSize`가 Chrome DevTools Protocol에서 누락되면 0으로 대체하지 않고 즉시 테스트를 실패시킨다.
- 수행한 검증:
  - `npm run typecheck`: pass.
  - `CI=1 COMINS_GRID_LAYOUT_PORT=6002 npm run verify:full`: pass — TypeScript lint, Vitest 5 files/19 tests, production build, Playwright 51 passed/10 skipped.
  - 기존 `6001` 재사용 환경에서 한 차례 `ERR_CONNECTION_REFUSED`가 발생했으나, 직전 반복 검증의 기존 dev server 종료와 겹친 하네스 경합으로 분리했다. 독립 포트·신규 server 검증에서는 재현되지 않았다.
  - 최종 전체 gate의 Chromium CDP counter (JS Heap byte, DOM Nodes, Event Listeners, Documents):

    | 구간 | JS Heap | DOM Nodes | Event Listeners | Documents |
    | --- | ---: | ---: | ---: | ---: |
    | column baseline (warm-up 이후) | 24,847,792 | 14,080 | 987 | 1 |
    | column cycle 1 | 24,530,352 | 14,080 | 987 | 1 |
    | column cycle 2 | 24,554,556 | 14,080 | 987 | 1 |
    | column cycle 3 | 24,611,412 | 14,080 | 987 | 1 |
    | interaction warmup (drag) | 27,203,564 | 14,091 | 1,000 | 2 |
    | interaction baseline (resize) | 27,426,676 | 14,093 | 1,000 | 2 |
    | repeated interaction 1 | 27,461,644 | 14,093 | 1,000 | 2 |
    | repeated interaction 2 | 27,341,484 | 14,093 | 1,000 | 2 |

- 결과:
  - 100-widget `1..12` column 반복은 JS Heap 단조 증가 없이 baseline 이하로 안정화했고, DOM Nodes/Event Listeners/Documents는 모든 측정에서 고정됐다.
  - drag/resize warm-up 이후 반복 interaction도 세 counter가 고정됐으며 JS Heap 최대·최종 2% 기준을 통과했다.
- 잔여 이슈 및 후속 작업:
  - Chrome DevTools GUI Performance Monitor 직접 연결은 이 환경에서 초기화 오류가 있어 미실행이다. 자동 Chromium CDP gate는 통과했으나 GUI 수동 확인은 필요 시 별도 환경에서 수행한다.
  - release, license, dependency version policy와 mobile desktop-only skip은 이번 안정화 범위에 포함하지 않는다.
