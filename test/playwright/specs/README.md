# Playwright Specs

Keep rendered interaction specs here. Tag only engine-sensitive parity scenarios with `@desktop-browser`, touch-only scenarios with `@mobile-touch`, and the 100-widget CDP scenario with `@resource-stability`; follow the canonical [resource stability gate](../../../docs/04-verification-strategy.md#100-widget-resource-stability-gate).
