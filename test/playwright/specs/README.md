# Playwright Specs

Rendered interaction specs live here. Cover dashboard rendering, drag, resize, maximize, minimize, restore, runtime column changes, and disabled interaction states.

Keep the 100-widget resource scenario in the `chromium-resource` project. It must return to the expected column and idle interaction state without cumulative DOM, event-listener, or document growth.
